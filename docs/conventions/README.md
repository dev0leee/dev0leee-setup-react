# 컨벤션

이 프로젝트의 코드 규칙 모음. **규칙은 코드에서 역으로 추출한 것**이지 이상론이 아니다.
문서와 코드가 어긋나면 코드가 맞고 문서가 틀린 것으로 본다 — 문서를 고치거나, 코드를 고치고 문서를 고친다.

## 읽는 순서

처음이면 `01` → `02` → `04` → `09` 만 읽어도 대부분의 코드를 쓸 수 있다.
나머지는 해당 작업을 할 때 찾아본다.

| #   | 문서                                      | 언제 읽나                    |
| --- | ----------------------------------------- | ---------------------------- |
| 01  | [폴더구조](./01-folder-structure.md)      | 파일을 새로 만들 때          |
| 02  | [네이밍](./02-naming.md)                  | 파일을 새로 만들 때          |
| 03  | [API 함수](./03-api.md)                   | 서버 통신 코드를 쓸 때       |
| 04  | [상태 관리](./04-state.md)                | 상태를 어디에 둘지 고민될 때 |
| 05  | [타입](./05-types.md)                     | 타입을 정의할 때             |
| 06  | [React](./06-react.md)                    | 컴포넌트/훅을 쓸 때          |
| 07  | [JavaScript](./07-javascript.md)          | 로직을 쓸 때                 |
| 08  | [라우팅/페이지](./08-routing.md)          | 페이지를 추가할 때           |
| 09  | [import/export](./09-imports.md)          | 항상                         |
| 10  | [컴포넌트](./10-components.md)            | 컴포넌트를 설계할 때         |
| 11  | [오버레이/모달/바텀시트](./11-overlay.md) | 모달을 띄울 때               |
| 12  | [상수 관리](./12-constants.md)            | 리터럴을 쓸 때               |
| 13  | [입력요소/인터랙션](./13-forms.md)        | 폼을 만들 때                 |
| 14  | [스타일링](./14-styling.md)               | 스타일을 쓸 때               |
| 15  | [브랜치/커밋/PR](./15-git.md)             | 작업을 시작하고 끝낼 때      |

## 규칙의 등급

각 문서는 세 등급으로 표기한다.

- **MUST** — 어기면 리뷰에서 반려. 대부분 린터/타입체커가 잡는다.
- **SHOULD** — 기본값. 어길 거면 PR 설명에 이유를 쓴다.
- **MAY** — 취향. 팀이 합의하면 바꾼다.

## 자동화되는 규칙

사람이 지킬 필요 없는 것들. 도구가 강제한다.

| 규칙                                           | 강제하는 도구                                         |
| ---------------------------------------------- | ----------------------------------------------------- |
| 세미콜론 없음, 홑따옴표, 100칸, trailing comma | Prettier (`.prettierrc.json`)                         |
| Tailwind 클래스 정렬                           | `prettier-plugin-tailwindcss`                         |
| import 그룹 순서 · 멤버 정렬                   | `@ianvs/prettier-plugin-sort-imports`                 |
| 상대경로 import 금지 (`@/` 강제)               | oxlint `no-restricted-imports`                        |
| `any` 금지, `import type` 강제, `eqeqeq`       | oxlint (`.oxlintrc.json`)                             |
| 훅 규칙, exhaustive-deps                       | oxlint `react/*`                                      |
| 미사용 변수/파라미터, 인덱스 접근 안전성       | tsconfig `noUnusedLocals`, `noUncheckedIndexedAccess` |
| 커밋 전 lint + format                          | husky + lint-staged                                   |

그래서 이 문서들은 **도구가 못 잡는 것**에 집중한다.
