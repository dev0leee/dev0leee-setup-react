# 마이그레이션 진행 상태

> **세션 시작 시 이 파일부터 읽고, 세션 종료 시 갱신한다.**
> 전체 계획은 `~/.claude/plans/working-smcom-apt-resident-fe-tranquil-charm.md`

## 명세 기준 (R1 — 고정)

| 항목         | 값                                                         |
| ------------ | ---------------------------------------------------------- |
| 레거시 레포  | `~/Desktop/working/smcom/apt-resident-fe`                  |
| 브랜치       | `dev`                                                      |
| **기준 SHA** | **`6d5bf22a1d4965e005477e46a66c5006d8e3e2e3`**             |
| 기준 시각    | 2026-07-27 10:13:17 +0900                                  |
| 기준 커밋    | `feat : 요일별 무료 주차 시간(주차 정책) 기능 개발 (#462)` |

**모든 명세는 이 시점의 코드를 기준으로 작성한다.**
기능 동결 합의가 없으므로(R1), 이 SHA 이후 레거시에 들어간 커밋은 아래 「기준 이후 레거시 변경」에
누적 기록하고 Phase 6 종료 시 일괄 반영한다.

확인 명령:

```bash
git -C ~/Desktop/working/smcom/apt-resident-fe log --oneline 6d5bf22..origin/dev
```

### 기준 이후 레거시 변경

| SHA      | 날짜 | 내용 | 영향 도메인 | 반영 여부 |
| -------- | ---- | ---- | ----------- | --------- |
| _(없음)_ |      |      |             |           |

---

## Phase 0 — 선결 결정

| #       | 항목                              | 상태     | 비고                                                                             |
| ------- | --------------------------------- | -------- | -------------------------------------------------------------------------------- |
| ~~0-1~~ | ~~인증 계약 백엔드 협의~~         | **해소** | 레거시 방식 유지 결정 (`decisions/auth-strategy.md`)                             |
| ~~0-2~~ | ~~네이티브 웹뷰 쿠키 검증~~       | **해소** | 쿠키 안 씀                                                                       |
| ~~0-3~~ | ~~자동 로그인 대체 설계~~         | **해소** | 레거시 자동 로그인 그대로 이식                                                   |
| ~~0-4~~ | ~~네이티브 브릿지 핸들러명 정합~~ | **해소** | 앱이 이미 배포됨 → **웹이 레거시 프로토콜에 맞춘다** (`native-protocol.md` §0)   |
| ~~0-5~~ | ~~캘린더·차트 대체재~~            | **해소** | shadcn `calendar` / `recharts` / `v-calendar` 제거 (`decisions/tech-choices.md`) |
| ~~0-6~~ | ~~opinion 빌드 형태~~             | **해소** | **멀티 엔트리 유지** — 경로 충돌 11건 + 외부 딥링크 보존 + env 스키마 분리       |
| ~~0-7~~ | ~~zod 3 → 4 차이 확인~~           | **완료** | `zod-migration.md` — `required_error` 33 + `invalid_type_error` 5 변환 필요      |

> 🎯 **Phase 0 종료. 결정 7건 전부 확정, 외부 블로커 0건.**

### 확정된 주요 결정

| 결정                                                                                                                                              | 문서                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| **인증은 레거시 방식 그대로 유지** (헤더 토큰 + localStorage + 자동 로그인). 쿠키 전환은 전환 후 별도 작업                                        | `decisions/auth-strategy.md`       |
| 인벤토리 확인 항목 11건 전부 확정                                                                                                                 | `decisions/inventory-questions.md` |
| **opinion 멀티 엔트리 유지** · 날짜선택기 shadcn `calendar` · 차트 `recharts` · `v-calendar` 제거                                                 | `decisions/tech-choices.md`        |
| **에러 모달은 Base UI Dialog로 레거시 재현** (`sweetalert2` 추가 안 함)                                                                           | 〃                                 |
| **의존성 8개 승인 · `@sentry/vite-plugin` 제외 · `lodash-es` 불필요** (2026-07-30)                                                                | `tech-mapping.md` §12              |
| **관리비 목업 화면(`/managementFee/info`)도 이관** — ApexCharts 2개를 recharts로 (2026-07-30)                                                     | `features/management-fee.md` §1    |
| **미생성 클래스를 현 Tailwind 토큰으로 표현** — `globalColor.scss` 팔레트를 되살리지 않는다. ⚠️ **화면을 바꾸라는 뜻이 아니다** (2026-07-30 정정) | `broken-styles.md` §0              |
| **QueryClient 기본값을 레거시에 맞춘다** — 전역 토스트 끄기, `throwOnError: false`, `retry: 0`, `staleTime: 0`                                    | 〃                                 |

**이관 제외 확정 (2026-07-29 사용자 재확인 — "사용처 없으면 쓰지마")**:
미출차 내역 **일체**(라우트·컴포넌트 3·훅 2·API 2·상수) · `postRejectCarRelease` ·
항상허용 차량 **수정** 기능 · `MyPageFontSizeItem.vue` · `v-calendar` ·
**콘텐츠 플래그 4개**(`hasAptCommunityContent`·`hasAptMovingHouseContent`·`hasAptVoteContent`·`hasAptShoppingContent`) ·
`FIXED_MENUS`의 `'공지사항'` · 죽은 `<style scoped>` 2건 · `useGetAptList`의 `watch` 무효화

**스타일 오타 수정 확정**: CSS 클래스 **26개** 중 21개를 이관 시 수정/삭제 → `broken-styles.md`
(서버·앱 계약 오타 4건은 **그대로 유지**, 협의 후 처리 → `deferred.md`)
⚠️ 1차 조사의 "16개"는 후보 생성이 불완전해 틀렸다. 2차에서 토큰 821개 전수 빌드 검증으로 확정(25 + `center` 1 = 26).

**이관 필수로 정정**: `getNoticeTopThree` — `(미사용중)` 주석은 낡은 것이고 메인 화면에서 실제 렌더 중.

> 판단 근거는 `decisions/inventory-questions.md`가 정본이다 (R-1 · R-2 · E-Q5b).

---

## Phase 1 — 계약 인벤토리

| 산출물               | 상태     | 실측 결과                                                                                                            |
| -------------------- | -------- | -------------------------------------------------------------------------------------------------------------------- |
| `routes.md`          | **완료** | **화면 라우트 121개** (메인 99 + opinion 18). 계획서의 "127"은 `path:` 항목 수 — 컨테이너 4·리다이렉트 1·주석 1 포함 |
| `endpoints.md`       | **완료** | **엔드포인트 148개** / 17개 모듈. 계획서의 "150"은 `axios.js`의 인스턴스 export 2개 포함                             |
| `native-protocol.md` | **완료** | **Web→App 17 / App→Web 7 = 24종.** 호출부 매핑 포함. 보존 필수 항목 11건                                             |
| `query-keys.md`      | **완료** | **훅 141개 / 쿼리 키 75종** (배열 56 + 무한목록 19). v4 `invalidateQueries` 28곳                                     |
| `domain-codes.md`    | **완료** | 상수 19파일·심볼 112개. **서버 에러코드 39종**, 정규식 7종                                                           |
| `env-vars.md`        | **완료** | 레거시 변수 11 + `MODE` 23곳. 타깃에 **8개 추가 필요**                                                               |

> **Phase 1 완료.** 산출물 6종 전부 작성됨.

### 실측으로 정정된 수치

| 항목           | 계획서 |           실측 | 사유                                                  |
| -------------- | -----: | -------------: | ----------------------------------------------------- |
| 라우트         |    127 |        **121** | 레이아웃 컨테이너 4 + 리다이렉트 1 + 주석 처리 1 제외 |
| API 엔드포인트 |    150 |        **148** | `axios.js`의 `client`·`auth` 인스턴스 export 2개 제외 |
| 쿼리 훅        |    142 |        **141** | 실측                                                  |
| 쿼리 키        |  42+19 | **56+19 = 75** | 배열 키를 단일행 grep으로 세어 멀티라인 21건 누락     |
| 브릿지         |     24 |         **24** | 일치                                                  |

---

## Phase 2 / 6 — 도메인별 상태

> **명세 밀도 방침 (2026-07-29 결정)**: 대칭·반복 구조도 **줄이지 않고 전수로 풀어쓴다.**
> 도메인 명세 하나만 보고 이관할 수 있어야 한다. 예상 총량 약 24,000줄.
>
> **명세 파일은 타깃 feature 슬라이스 기준으로 묶는다.** `IntroView` + `LoginView`는
> 기능상 한 도메인이라 `features/auth.md` 하나로 작성했다.

| 도메인            | `.vue` |  LOC | 명세     | 이관     | PR                                                               |
| ----------------- | -----: | ---: | -------- | -------- | ---------------------------------------------------------------- |
| Login             |      5 |  463 | **완료** | **완료** | A1~A4 `40659c4` · A5·A6 SignUp과 함께                            |
| Intro             |      2 |  121 | **완료** | **완료** | ← `40659c4` (A1)                                                 |
| SignUp            |      8 |  703 | **완료** | **완료** | S1~S5 + KMC 인증 · 약관 동의 폼                                  |
| Exception         |      4 |  149 | **완료** | 부분     | E1·E2·E3 완료 (`aabe9dd`). opinion E4~E7은 opinion 엔트리와 함께 |
| Main              |     22 | 1708 | **완료** | **완료** | 공지·투표 팝업만 보류 (Board·Vote 이관 후)                       |
| MyPage            |     19 | 1149 | **완료** | **완료** | ← Phase 5 파일럿 (`10057cf`)                                     |
| Board             |     48 | 3642 | **완료** | **완료** | 21개 화면 전부 (1/4~4/4)                                         |
| ParkingManagement |     31 | 3278 | **완료** | 진행중   | **11/15 화면.** 방문예약 4개(PK11~PK14)만 남음                   |
| Visit             |     23 | 2042 | **완료** | 진행중   | **3/13 화면** (V1·V2·V3). 임시비밀번호·QR·안면인식 남음          |
| Vote              |     23 | 1673 | **완료** | 미착수   |                                                                  |
| Survey            |     19 | 1324 | **완료** | 미착수   |                                                                  |
| AptMall           |     19 | 1472 | **완료** | 미착수   |                                                                  |
| FireInspection    |     13 | 1231 | **완료** | 미착수   |                                                                  |
| MovingHouse       |     10 | 1071 | **완료** | 미착수   |                                                                  |
| Repair            |     11 |  964 | **완료** | 미착수   |                                                                  |
| ManagementFee     |      3 |  906 | **완료** | 미착수   |                                                                  |
| Apass             |      3 |  301 | **완료** | 미착수   |                                                                  |
| TermsOfUse        |      3 |  158 | **완료** | **완료** | `features/terms` — 마이페이지 P6의 404가 해소됐다                |
| opinion 앱        |      — |    — | **완료** | 미착수   |                                                                  |

---

## 미해결 질문 (사용자 확인 대기)

**막힌 작업 없음.** 아래는 통보·확인 수준이며 진행을 막지 않는다.

| #    | 항목                                                               | 성격                                                                         |
| ---- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| M-Q1 | `SMA0002` 비컨 우회 코드가 아직 필요한지 (앱 이슈 해결 여부)       | 앱 팀 확인. **현행 유지**. 불필요하면 `useBeaconWorkaround` 삭제             |
| M-Q5 | `pushAuthorized`를 저장만 하고 안 쓰는 게 맞는지                   | 확인 필요. **현행 유지**(이관본은 저장 안 함)                                |
| N-Q1 | 앱 팀에 브릿지 프로토콜 변경 계획이 있는지 통보 겸 확인            | 통보. 없으면 그대로 진행                                                     |
| N-Q3 | 앱 종료 상태 푸시가 라우터 준비 전에 도착하는지                    | ✅ **해소** — 큐 + 이벤트로 라우터 마운트 전 푸시를 보존한다 (Phase 4 4단계) |
| P-Q1 | `소방 자가 점검`이 마이페이지에서는 조건 없이 보이는 것이 의도인가 | 레거시 그대로 이관함. 확인만 필요                                            |
| P-Q2 | `localStorage['version']`을 네이티브 앱이 직접 쓰는가              | 앱 팀 확인. 안 쓴다면 앱 버전 표시는 죽은 기능이다                           |

> 인벤토리 확인 항목 11건은 **전부 확정**됐다 → `decisions/inventory-questions.md`

### ✅ 이관 착수 전 결정 — **2026-07-31 전건 확정**

**"고치면 화면/동작이 달라지는" 결함들이다.** 등가 이관 원칙만으로는 판단할 수 없어
사용자 결정을 받았다. **미결 0건.**

| #               | 결함                                                                              | 결정                                                                                   | 문서                    |
| --------------- | --------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ----------------------- |
| `RP-Q4`         | 하자보수 목록의 무한 스크롤 관측 타깃이 템플릿에 없다                             | ✅ **고친다** — 11번째 이후 항목이 처음으로 보이게 된다                                | `repair.md` §4          |
| `RP-Q1`         | 등록 화면 AppBar 제목이 `하자보수 수정`                                           | ✅ **`하자보수 등록`으로 고친다**                                                      | `repair.md` §1          |
| `RP-Q2`         | 등록 화면에 AppBar가 2개 겹친다                                                   | ✅ **고친다** — 하나만 남긴다                                                          | `repair.md` §2          |
| `RP-Q3`         | 수정 화면 콘텐츠 최상단 48px이 AppBar에 가린다                                    | ✅ **고친다** — 가려진 내용을 드러낸다                                                 | `repair.md` §3          |
| `AP-Q3`         | A-PASS 7초 타임아웃이 전역 로딩 플래그를 못 내려 **뒤로가기 영구 차단**           | ✅ **고친다** — 전환 중 7초는 그대로 막고, **타임아웃 시점에 앱 응답과 무관하게 푼다** | `apass.md` AP1          |
| `F-Q14`         | 소방 `홈으로 돌아가기`가 `/` 경유 → `getLoginInfo()` 실패 시 **로그아웃**         | ✅ **`/main` 직행으로 고친다** — 투표·설문 완료 화면과 같아진다                        | `fire-inspection.md` F3 |
| `O-Q6`          | opinion 레이아웃 2중 중첩 (`pt-6`×2=48px에 의존)                                  | ✅ **고친다** — 중첩 제거 + `pt-12`로 함께 맞춘다                                      | `opinion.md` §4         |
| `O-Q7`          | opinion 앱에 `ToastContainer`가 없어 **모든 토스트 무음**                         | ✅ **고친다** — 레거시에 없던 토스트가 나타난다                                        | `opinion.md` §4         |
| `VT-Q2`·`SV-Q3` | Vote·Survey의 `isCreateXxxFormPending` 오타 (같은 버그)                           | ✅ **고친다** — 중복 제출 잠금이 생긴다                                                | `vote.md`·`survey.md`   |
| `MH-Q10`        | 이사예약 안내문이 `<p>` 안의 `<div>` 때문에 배경·패딩 손실                        | ✅ **고친다** — 회색 카드 배경이 살아난다                                              | `moving-house.md` MH2   |
| `PK-Q2`         | `history.state.forward` 기반 "상세 복귀 감지"를 react-router에서 재현             | ✅ **A안** — 떠날 때 향한 경로를 **목록별 키로 저장**해 판정                           | `parking.md` §3-4       |
| `PK-Q11`        | `case 'RESERVATION_DATE_INVALID('` 오타로 방문예약 **삭제** 실패 문구가 서버 원문 | ✅ **고친다** — `방문예약 기간 설정은 최대 7일입니다.`가 뜬다                          | `parking.md` §PK14      |
| ~~`MF-Q1`~~     | ~~관리비 `/info` 목업~~                                                           | ✅ **이관 확정** (2026-07-30) — `§1-2`에 전수 명세                                     | `management-fee.md` §1  |

> `invalidateQueries` v4 **28곳은 결정 대상이 아니다** — 안 고치면 화면이 갱신되지 않아
> 이미 전부 v5 시그니처로 옮기고 있다 (`query-keys.md`).

---------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------- | ----------------------- |
| `RP-Q4` | 하자보수 목록의 무한 스크롤 관측 타깃이 템플릿에 없다 | **지금까지 안 보였던 11번째 이후 항목이 보인다** | `repair.md` §4 |
| `RP-Q1` | 등록 화면 제목이 `하자보수 수정`, 모달이 `수정 그만두기` | 제목·모달 문구가 바뀐다 | `repair.md` §1 |
| `RP-Q2` | 등록 화면에 AppBar 2개 · `RP-Q3` 수정 화면 콘텐츠 48px 가림 | 레이아웃이 바뀐다 | `repair.md` §2·§3 |
| `AP-Q3` | A-PASS 7초 타임아웃이 전역 로딩 플래그를 못 내려 **뒤로가기 영구 차단** | 뒤로가기가 동작하기 시작한다 | `apass.md` AP1 |
| `O-Q6` | opinion 레이아웃 2중 중첩 (`pt-6`×2=48px에 의존) | 중첩 제거 시 **`pt-12`로 함께 고쳐야** 한다 | `opinion.md` §4 |
| `O-Q7` | opinion 앱에 `ToastContainer`가 없어 **모든 토스트 무음** | **레거시에 없던 토스트가 나타난다** | `opinion.md` §4 |
| ~~`MF-Q1`~~ | ~~관리비 `/info` 목업~~ | ✅ **이관 확정** (2026-07-30) — `§1-2`에 전수 명세 | `management-fee.md` §1 |
| `VT-Q2`·`SV-Q3` | Vote·Survey의 `isCreateXxxFormPending` 오타 (같은 버그) | 중복 제출 잠금이 생긴다 | `vote.md`·`survey.md` |
| `MH-Q10` | 이사예약 안내문이 `<p>` 안의 `<div>` 때문에 배경·패딩 손실 | 회색 카드 배경이 살아난다 | `moving-house.md` MH2 |
| `F-Q14` | 소방 `홈으로 돌아가기`가 인트로 경유 → `getLoginInfo()` 실패 시 **로그아웃** | 3홉 우회가 없어진다 | `fire-inspection.md` F3 |
| v4 `invalidateQueries` | **28곳이 v5에서 no-op.** 특히 `AP-Q`(A-PASS 토글)는 **UI 갱신 경로가 이것뿐** | 화면이 즉시 갱신되기 시작한다 | `query-keys.md` |

> **v4 `invalidateQueries`는 "고쳐야 하는" 쪽이다** — 안 고치면 화면이 갱신되지 않는다.
> 나머지는 **레거시가 이미 그렇게 동작하고 있으므로** 사용자 결정이 필요하다.

---

## Phase 4 — 기반 구축 ✅ **완료 (2026-07-30)**

`tech-mapping.md` §14의 11단계를 전부 수행했다. 커밋 8개.

| #      | 단계                  | 결과                                                                                                                 |
| ------ | --------------------- | -------------------------------------------------------------------------------------------------------------------- |
| **1**  | 의존성 · env · eslint | 8개 설치 + shadcn 12종 · env 스키마 2분할(opinion 대응) · `FEATURE_SLICES` 18개 zone                                 |
| **2**  | 디자인 토큰           | 색 207 + 타이포 67 + `--font-scale` + `sm:392px` + `public/assets` 210개. **v3→v4 클래스 변경 4종 55곳 실측**(§10-1) |
| **3**  | HTTP 레이어           | 헤더 토큰 · errorCode 재발급 트리거 · 대기 큐 · 자동 로그인. 테스트 19개                                             |
| **4**  | 네이티브 브릿지       | `JsInterface` · `{type,data}` · iOS 객체/Android 문자열 · `window.CALLBACK_*` 7종. 테스트 14개                       |
| **5**  | 인증 슬라이스         | 로그인 에러코드 분기 · `loginDataHandler` · 로그아웃 플로우 · 자동 로그인 구동부                                     |
| **6**  | aptInfo 배치          | `authStore`가 소유. 병합 semantics·동기 접근 유지 (D-35)                                                             |
| **7**  | 오버레이 · 에러 모달  | `showErrorModal()`(호출부 293곳 대체) · `ModalBase` · `DrawerBase`. 테스트 7개                                       |
| **8**  | 공용 컴포넌트 38개    | 30개 재작성 + 레이아웃 5개 + Toast 2개는 sonner. `LayoutOpinionBase`만 Phase 6으로                                   |
| **9**  | 레이아웃 셸           | `AppBar`·`BottomNavigation`·`RootLayout`·`AppLayout` + route `handle` 메타 + 뒤로가기 라우팅표                       |
| **10** | `useInfiniteList`     | 페이지 크기 10 · `pages[0]` pageable · 접두사 `resetCache`. 테스트 6개                                               |
| **11** | 데모 제거             | `features/dashboard` 삭제 · MSW 핸들러를 실제 인증 계약으로 교체                                                     |

### 완료 조건 검증

`src/app/router.test.tsx`가 **실제 `routes` 트리로** 사슬 전체를 통과시킨다:

```
폼 제출 → 헤더 토큰 저장(레거시 키) → login/info + 단지목록 병렬 조회
→ aptInfo 적재 → SEND_INITIAL_RESIDENT_INFO 발신(' 로비폰 ' trim 확인)
→ 라우터 가드 통과 → 하단 탭 렌더
```

`pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` 통과 — **테스트 51개**.

### ⚠️ Phase 4에서 끝내지 못한 것

| 항목                       | 이유                                                                                                                   |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| **`.env.*` 파일 갱신**     | 세션 권한이 `.env*` 읽기·쓰기를 차단한다. 추가할 변수는 `env-vars.md` §7. **이것 없이는 `pnpm dev`가 부팅하지 않는다** |
| **실기기 확인**            | 브릿지 24종은 앱 안에서만 검증된다. MSW 테스트는 웹 쪽 계약만 고정한다                                                 |
| **픽셀 대조**              | 에러 모달(SweetAlert2 재현)·토스트·칩 18색은 레거시와 나란히 띄워 봐야 한다 (R10)                                      |
| **opinion 엔트리**         | `main-opinion.tsx`·`routerOpinion.tsx`는 Vote·Survey 이관과 함께 (`opinion.md`)                                        |
| **`vue-quill.snow.css`**   | 전역이 아니라 `features/board`와 함께 (§10-2)                                                                          |
| **날짜 선택기 래퍼**       | `shadcn calendar`는 설치했지만 래퍼는 AptMall AM9 기준으로 만든다 (`moving-house.md` MH-Q12)                           |
| **`ImageUploader`·진행률** | Board 이관과 함께 (`repair.md` 「이관 순서」)                                                                          |

---

## Phase 5 — 파일럿 도메인 (MyPage) ✅ **완료 (2026-07-30)**

레거시 `MyPageView/` 19파일 1,149 LOC → 화면 8개. 커밋 3개(`3df0400` · `aabe9dd` · `10057cf`).

| 화면              | 경로                      | 특징                                                    |
| ----------------- | ------------------------- | ------------------------------------------------------- |
| P1 마이페이지     | `/mypage`                 | 구독 콘텐츠 2단 필터 · 앱 버전 · 하단 탭                |
| P2 내 프로필      | `/mypage/profile`         | 화면 안 AppBar + 우측 `수정`                            |
| P3 내 프로필 수정 | `/mypage/profile/edit`    | RHF + Controller · 폼 밖 읽기전용 필드 · 비밀번호 모달  |
| P4 알림 설정      | `/mypage/alarmSetting`    | 훅 8개 조합 · mutation 응답 우선 · 마케팅↔광고성 양방향 |
| P5 관리사무소     | `/mypage/aptInfo`         | `tel:` 링크 · `HH:mm` 절단 · 단지 로고 폴백             |
| P6 약관 및 정책   | `/mypage/termsOfUse`      | `title` 표시(`label` 아님)                              |
| P7 글자 크기 설정 | `/mypage/fontSizeSetting` | 의사요소 CSS · 앱 전역 `--font-scale`                   |
| P8 회원 탈퇴      | `/mypage/accountDeletion` | 로비폰 통보 실패해도 세션 정리                          |
| A7 로그아웃       | `/logout`                 | 화면 없는 확인 모달 → `features/auth`                   |

### 확정된 레시피 → `recipe.md`

| §   | 확정한 것                                                                                   |
| --- | ------------------------------------------------------------------------------------------- |
| 2   | **`shared` 승격 기준** — "도메인 규칙인가, 앱이 딛고 서는 사실인가". 단지 컨텍스트를 올렸다 |
| 3   | 라우트 메타는 `handle` · 기본값 재기술 금지 · **배럴 정적 import 시 lazy 무효** 확인        |
| 4   | 쿼리 키 팩토리(접두사+전체) · `enabled` 가드 · mutation 응답 우선 패턴 · `!= null`          |
| 5   | **`watch()` → React 변환 7행 판단표** (렌더 계산 → 핸들러 → effect 순)                      |
| 6   | **제어 컴포넌트는 `Controller`** · 버튼 활성은 `errors` 비어 있음 · `mode: onChange`        |
| 7   | 오버레이 4종 사용 규격 · 라우트가 모달인 경우                                               |
| 8   | 레거시 클래스 그대로 · **의사요소는 `index.css` 원시 CSS**                                  |
| 10  | 테스트 범위 5종 · 로그인 흐름 재사용 금지(스토어 직접 주입)                                 |

### 🔴 파일럿에서 실제로 잡은 React 번역 버그

**동의 토스트 stale closure.** 이미 만들어진 핸들러 클로저는 재렌더된 mutation 응답을 볼 수
없어 **이전 동의 일시**를 보여줬다. Vue는 `getFlag`가 `ref`를 읽어 문제가 없던 자리다.
`mutateAsync` 반환값을 인자로 넘겨 해결. **테스트가 잡았고, 조용히 틀리는 종류였다.**
→ `recipe.md` §4에 규약으로 명문화했다.

### 부수 작업 (파일럿에서 필요해진 것)

| 작업                                | 이유                                                              |
| ----------------------------------- | ----------------------------------------------------------------- |
| 단지 컨텍스트 4종을 `shared`로 승격 | 레거시 사용처 23개 파일. feature에 두면 11개 도메인이 침범한다    |
| `features/exception` E1·E2·E3       | 단지 컨텍스트 조회 실패가 `/error-auth`로 보낸다. 라우트가 없었다 |
| 템플릿 `app/NotFoundPage.tsx` 교체  | 영문·shadcn 버튼이라 레거시와 달랐다                              |
| `showToast`가 `ReactNode`를 받게    | 동의 토스트에 `<br />`이 필요하다 (P-Q4)                          |
| `InputBaseType`에 `password` 추가   | 모달이 `InputPassword`가 아니라 `InputBase`를 쓴다                |
| `formatDay` · `compareSemver` 이식  | 관리사무소 · 앱 버전                                              |
| `shared/constants/terms.ts`         | 약관 4종을 signup·mypage가 함께 쓴다                              |

### 검증

`pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build` 통과.
**테스트 83개**(mypage 29개 신규). 코드 스플리팅 실측: `mypage` 45.4kB · `exception` 1.7kB 분리.

### ⚠️ Phase 5에서 끝내지 못한 것

| 항목                                 | 이유                                                                                                                                     |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| **UI 픽셀 대조**                     | 레거시와 나란히 띄워 8화면 대조 필요 (R10). 특히 `text-brand-primary-50` 매핑으로 **마이페이지 그룹 제목이 검정 → 파랑**으로 바뀌는 자리 |
| **실기기 확인**                      | `tel:` 링크 · `GET_APP_VERSION` 왕복 · 글자 배율 5단계                                                                                   |
| **`/termsOfUse/:id` 약관 본문 화면** | P1·P6의 이동 대상. **signup 도메인 소유**라 Phase 6에서 만든다 — 지금 누르면 404다                                                       |
| **레시피 미정 8건**                  | 스크롤 위치 복원 · 이미지 업로드 · 폼 Context 등 → `recipe.md` §12. 처음 필요한 도메인에서 확정                                          |

---

## Phase 6 — 도메인별 이관 (진행 중)

### auth A1~A4 ✅ **완료 (2026-07-30, `40659c4`)**

| 화면                      | 비고                                                                       |
| ------------------------- | -------------------------------------------------------------------------- |
| A1 `/intro`               | 템플릿 shadcn 카드를 레거시 마크업으로 교체. `LoginPage` → `IntroPage`     |
| A2 `/password/cert`       | 2단 폼 · 180초 타이머 · **1회 제한 재요청**(A-Q1 그대로 재현)              |
| A3 `/password/reset`      | 인증 토큰은 라우터 state로만. 읽은 뒤 비워 새로고침 시 `/`로 튕긴다        |
| A4 `/login/pending`       | 승인 대기. 진입 전 FCM 토큰 등록 발신은 `usePatchLogin`이 이미 하고 있었다 |
| ~~A5·A6~~ `/versionOne/*` | **SignUp과 함께** — `TermsOfUseAgreeView`(약관 동의)와 KMC 인증에 의존한다 |

**라우터 가드도 함께 완성했다.** A1의 동작이 가드에 정의돼 있어 분리할 수 없다.

| 레거시 가드 단계                        | 타깃                                                  |
| --------------------------------------- | ----------------------------------------------------- |
| `requiresAuth && 미인증`                | `ProtectedRoute` (Phase 4에서 완료)                   |
| `requiresAuth === false && 인증됨`      | **`app/publicRouteLoader.ts`** (신규)                 |
| `return false` (오프라인·뒤로가기 차단) | **`app/navigationBlocking.ts` + `useBlocker`** (신규) |

> **가드를 컴포넌트가 아니라 `loader`로 옮긴 이유**: 인트로는 마운트 시 `clearAuth()`를
> 부른다. 컴포넌트로 만들면 **정상 세션이 지워진 뒤에** 메인으로 이동한다.
> `recipe.md` §3에 규칙으로 넣었다.

#### ⚠️ 이 단계에서 끝내지 못한 것

| 항목                              | 이유                                                                                                                                                                                    |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **UI 픽셀 대조**                  | A1~A4 4화면을 레거시와 나란히 봐야 한다 (R10)                                                                                                                                           |
| **`/signup/terms` 링크**          | 인트로의 `회원가입`이 아직 404다. SignUp 이관 때 이어진다                                                                                                                               |
| **`publicRouteLoader` 적용 범위** | 지금은 **auth 공개 라우트 4개에만** 걸었다. 레거시 가드는 `*`(404)·`/signup/*`에도 적용된다 — **로그인 상태로 없는 경로에 가면 404 대신 `/main`으로 간다.** 각 도메인 이관 때 함께 건다 |
| **오프라인 차단 실기기 확인**     | `useBlocker`로 재현했지만 웹뷰에서 `navigator.onLine`이 정확한지는 기기에서만 안다                                                                                                      |

### signup + auth A5·A6 ✅ **완료 (2026-07-30)**

| 화면                                | 비고                                                  |
| ----------------------------------- | ----------------------------------------------------- |
| S1 `/signup/terms`                  | 약관 동의 + KMC 진입. 폼은 `TermsAgreeForm`(공용)     |
| S2 `/signup/certification/response` | 쿼리스트링 → 스토어. 화면 요소 없음                   |
| S3 `/signup/info/user`              | 이름·닉네임·비밀번호. 뒤로가기 확인 모달              |
| S4 `/signup/info/apt`               | 아파트 검색 모달 · 동/호수 · 세대주. 14필드 제출      |
| S5 `/signup/completed`              | 승인 대기 안내                                        |
| T1 `/termsOfUse/:termsId`           | `features/terms` — **마이페이지 P6의 404가 해소됐다** |
| A5 `/versionOne/terms`              | S1과 같은 폼, 버튼 문구·콜백만 다르다                 |
| A6 `/versionOne/terms/response`     | 전환 제출. 실패 시 토큰·단지정보만 지우고 `/`로       |

**`shared`로 올린 것** — 두 도메인 이상이 쓰는 것만 (`recipe.md` §2 기준):

| 대상                                                | 쓰는 곳                           |
| --------------------------------------------------- | --------------------------------- |
| `CertButton` · `lib/certification.ts`               | signup · auth(A5) · vote · survey |
| `TermsAgreeForm` · `useTermsAgreement`              | signup(S1) · auth(A5)             |
| `useWaitingMemberFcmToken` · `lib/waitingMember.ts` | auth(A1-2) · signup(S4)           |
| `SIGNUP_ERROR_MESSAGE` · `SIGNUP_ERROR_CODE`        | signup(S4) · auth(A6) — 같은 표   |
| `LoginInfo` 타입                                    | 위 두 곳이 같은 응답 모양을 쓴다  |

#### 🔴 실측으로 정정한 명세 2건

| 항목                     | 명세                             | 실제                                                                                                         |
| ------------------------ | -------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `SignUpAptInfoRadio.vue` | S4의 세대주 라디오               | **import 0곳 — 죽은 파일.** S4는 공용 `InputRadioDual`을 쓴다. `bg-primary-400`(B-Q1)의 **적용 자리가 없다** |
| S3 라우트 meta           | `showAppBar:false` + 화면이 렌더 | meta가 **`true`** 라 **AppBar가 2개 겹친다.** S4만 `false` — 비대칭 (D-212)                                  |

#### ⚠️ 이 단계에서 끝내지 못한 것

| 항목                        | 이유                                                                                    |
| --------------------------- | --------------------------------------------------------------------------------------- |
| **KMC 실기기 확인 (S-Q3)**  | 외부 폼 POST가 웹뷰 안에서 열리는지, 콜백이 앱으로 돌아오는지는 **기기에서만 확인된다** |
| **UI 픽셀 대조**            | 8화면. 검색 모달과 세대주 라디오를 특히 봐야 한다                                       |
| **S-Q4 KMC 콜백 필드 목록** | `...query`로 전부 받고 있어 정확한 목록은 미확인. 실기기 왕복 로그로 확정 가능          |

### main 1/3 — 골격·헤더·단지 전환 ✅ **완료 (2026-07-30)**

| 구성요소                           | 상태                                                          |
| ---------------------------------- | ------------------------------------------------------------- |
| `MainPage` 골격 + 권한 정보 요청   | ✅                                                            |
| 비컨 우회 코드(`SMA0002`·개발모드) | ✅ `once` 재현                                                |
| `AptInfoHeader` + 어절 줄바꿈      | ✅                                                            |
| `AptInfoHeaderDrawer` + `Item`     | ✅ 로딩·에러·정상 3상태                                       |
| `useChangeApt` 경쟁 조건 해소      | ✅ `fetchQuery` 응답에서 직접 읽는다 (`main.md` §5 확정 방침) |

**`useChangeApt`를 고쳤다.** 레거시는 무효화 뒤 스토어를 읽으며 "다른 훅의 `watch`가
이미 새 값을 썼을 것"이라고 가정한다. React에서는 `await`가 `useEffect`보다 먼저 풀려
**거의 항상 `contentList: []`를 읽어 A-PASS·로비폰이 둘 다 false로 앱에 간다.**
`fetchQuery` 응답에서 직접 읽어 순서 의존을 없앴다 — 요청 1회, 정상 동작 시 결과 동일.

#### 🔴 실측/추론으로 정정한 명세 1건

`승인되지 않은 단지입니다.` 모달은 **레거시에서 뜨지 않는다.** `selectApt`가 드로어를 먼저
닫고, 부모의 `v-if`가 모달 상태를 가진 컴포넌트를 언마운트한다. 미승인 단지를 누르면
드로어만 닫히고 아무 안내가 없다 (D-216). 이관본은 그 동작을 재현했다.

### main 2/3 — 카드 그리드·카드 7종 ✅ **완료 (2026-07-30, `f5206bc`)**

| 구성요소                        | 상태                                                                |
| ------------------------------- | ------------------------------------------------------------------- |
| `useMainCardLayout` (242 LOC)   | ✅ 개수별 배치 프리셋을 **테스트 8개로 고정**                       |
| 카드 5종 + A-PAY 2카드          | ✅                                                                  |
| ApexCharts radialBar → recharts | ✅ 각도 기준이 달라 `startAngle 90 / endAngle -270`으로 변환        |
| 레거시 버그 2건 수정            | ✅ 마일리지 쿼리 키에 uuid 없음(D-217) · 주차 구독 검사 누락(D-218) |

카드 수는 **0·2·3·4·5장만** 가능하다 — 마일리지와 방문예약이 같은 조건(주차 구독)이라
항상 함께 나오기 때문이다. 4장이면서 관리비 미구독일 때만 별도 순서 프리셋을 쓴다.

### main 3/3 — 스와이퍼·배너·공지·쇼핑 동의 ✅ **완료 (2026-07-31)**

| 구성요소                                           | 상태                                                          |
| -------------------------------------------------- | ------------------------------------------------------------- |
| `MainNavigationSwiper` (swiper/react + Pagination) | ✅ 8개 단위 슬라이드, New 배지, 클릭 4분기                    |
| `filterSwiperMenuList` (메뉴 노출 규칙)            | ✅ 테스트 7개로 고정                                          |
| `MainAdvertisementBanner`                          | ✅ 단지 UUID 3분기 + `Ad` 배지 조건                           |
| `MainNoticeTopThree`                               | ✅ 로딩·에러·빈 상태·정상 4상태, `<li class="contents">` 재현 |
| `MainShoppingTermsBottomSheet` + `ModalPage`       | ✅ `initialValue: true`, 동의/거절 모두 쇼핑몰 진입           |
| `useShoppingNavigation`                            | ✅ 앱이면 새 웹뷰, 웹이면 새 탭                               |

**옮기지 않은 것 3건 (전부 레거시에서 죽어 있다):**

| 항목                                      | 이유                                                                   |
| ----------------------------------------- | ---------------------------------------------------------------------- |
| `MainNotOutHistory.vue`                   | `MainView.vue`에서 import·사용이 **주석 처리**돼 있다                  |
| `FIXED_MENUS`의 `'공지사항'`              | `MAIN_SWIPER_MENU_LIST`에 없는 이름이라 아무것도 매칭되지 않는다(D-38) |
| `useGetNoticeTopThree`의 `recentlyNotice` | `select` 안에서 ref에 담기만 하고 읽는 곳이 없다                       |

**레거시 불일치를 그대로 옮긴 것:**

- 스와이퍼의 `contentName: '투표'` vs 카드 게이트의 `'전자투표'` (M-Q3) — 테스트로 못박았다
- 약관 본문 모달의 헤더 제목이 빈 문자열 (D-222)
- 마케팅 동의 저장 후 상세정보 무효화 없음 (D-221)
- 공지 제목이 `formatHtmlText` 결과를 **텍스트로** 출력해 `<br/>`이 글자로 보인다

**의존성**: `swiper@14`는 기설치 상태였다. `lodash`는 추가하지 않고 `chunk`를 직접 만들었다
(`shared/utils/chunk.ts` — 3줄).

#### ⚠️ main에 남은 것

| 단계 | 범위                                                                |
| ---- | ------------------------------------------------------------------- |
| 보류 | 공지 팝업(**Board**) · 투표 대기 팝업(**Vote**) — 그 도메인 이관 후 |

> 붙일 때 **투표 팝업을 먼저, 공지 팝업을 나중에** 렌더한다. 둘의 z-index가 같아
> DOM 순서가 곧 우선순위이고 레거시는 공지가 위에 오게 뒀다 (`MainPage.tsx` 주석).

---

### board 1/4 — 공지 계보 ✅ **완료 (2026-07-31)**

Board는 21화면이라 4단계로 나눈다. **1/4는 공지 계보** — 끝내면 메인의 공지 팝업도 붙는다.

| 화면                    | 상태                                                             |
| ----------------------- | ---------------------------------------------------------------- |
| B1 공지사항 목록        | ✅ 검색(debounce 500ms) · 카테고리 탭 · 무한스크롤 · 스크롤 복원 |
| B2 공지사항 상세        | ✅ Quill 본문 · 첨부 · 이미지 뷰어 · 외부 링크 브릿지            |
| B3 아파트먼트 공지 목록 | ✅ 검색만(카테고리 없음) · 공용 스크롤 복원                      |
| B4 아파트먼트 공지 상세 | ✅ **제목이 평문**이고 첨부 필드명이 B2와 다르다                 |
| B21 공지 팝업           | ✅ `app/MainScreen`이 메인과 함께 조립                           |

**함께 만든 공용 하부구조 5개**: `convertDeltaToHtml` · `quill.snow.css`(937줄) ·
`formatIsoStringDate` · `useIntersectionObserver` · `useInfiniteScrollPosition`.

#### 이관 중 확정한 것

| 항목                        | 결정                                                                                          |
| --------------------------- | --------------------------------------------------------------------------------------------- |
| **BD-Q3** 글자별 `<span>`   | **재현하지 않는다.** 렌더 결과가 같고 스크린리더가 제목을 한 글자씩 읽어 오히려 해롭다        |
| BD-Q5 파라미터 순서 캐시 키 | Phase 4에서 이미 "그대로 유지"로 결정돼 있었다 — 추가 결정 불필요                             |
| 팝업 배경 클릭              | 레거시가 `@close`를 연결하지 않아 **닫히지 않는다.** `onClose`를 no-op으로 두어 재현          |
| B3 로딩 표시                | 레거시 스피너는 **도달 불가 코드**다(부모가 로딩 중엔 목록을 안 그린다). 스피너를 넣지 않았다 |

#### 🔴 실측으로 잡은 레거시 결함 1건

`convertDeltaToHtml`이 **`[{`로 시작하는 Delta에서 반드시 실패한다** — 배열 검사보다
`parsedDelta.ops.some(...)`이 먼저 실행돼 TypeError가 난다. 그 형식을 위한 분기를 만들어
놓고 처리하지 못하는 셈이고, 화면에는 `정보없음`이 뜬다. **실패를 그대로 재현**하고
D-223에 남겼다 — 고치면 지금까지 안 보이던 본문이 나타나 화면이 달라진다.

### board 2/4 — 소통공간·민원공간 목록 + 내 활동 ✅ **완료 (2026-07-31)**

> **분할을 조정했다.** 상세 화면은 댓글과 한 몸이라 나눌 수 없다 —
> 2/4를 목록 계열로 좁히고 3/4에 상세+댓글을 함께 묶었다.

| 화면              | 상태                                                    |
| ----------------- | ------------------------------------------------------- |
| B5 소통공간 목록  | ✅ 검색·카테고리·무한스크롤·스크롤 복원·글쓰기 버튼     |
| B12 민원공간 목록 | ✅ + 처리상태 칩 · 자물쇠(비밀글)                       |
| B11·B18 내 활동   | ✅ 탭 2종. 두 목록을 진입 시 함께 조회한다(레거시 동일) |

**두 게시판을 한 컴포넌트로 합쳤다** — 이 화면들의 차이는 라벨과 경로뿐이다.
§4의 17개 차이는 상세·폼·에러 처리 쪽이라 3/4·4/4에서 갈린다.

**함께 만든 공용 하부구조**: `formatKoreanTimeAgo` + `useKoreanTimeAgo`.

#### 🔴 이관 중 잡은 내 버그 1건 (레거시 아님)

`useIntersectionObserver`를 `useRef` + 빈 deps로 만들어서 **센티널이 나중에
나타나는 화면에서 무한스크롤이 동작하지 않았다.** 목록이 비어 있는 동안에는
센티널이 렌더되지 않아 effect가 `null`을 보고 끝났고, 데이터가 도착해도 다시 걸지
않았다. 콜백 ref로 바꿔 엘리먼트가 붙는 순간 관찰을 시작하게 고쳤다.
**Board 1/4의 공지 목록(B1)도 같은 이유로 다음 페이지를 못 부르고 있었다** —
이번 수정으로 함께 해결됐다.

#### 시간 표기는 실측으로 맞췄다

`useKoreanTimeAgo`는 vueuse `useTimeAgo`의 단위 경계에 의존한다(46분·20시간·6일…).
**임계값을 짐작하지 않고 레거시 레포의 실제 `@vueuse/core`를 실행해 18개 값을 뽑아
테스트 기준표로 넣었다.** 45분 59초가 `46분 전`으로 반올림되는 것까지 일치한다.

### board 3/4 — 상세 + 댓글 ✅ **완료 (2026-07-31)**

이 도메인에서 가장 큰 덩어리다. 더보기 드로어(199 LOC)·좋아요·댓글 4화면이 여기 모여 있다.

| 화면               | 상태                                                           |
| ------------------ | -------------------------------------------------------------- |
| B6·B13 게시글 상세 | ✅ 본문·첨부·좋아요·더보기(수정/삭제/차단/신고)·댓글 목록·입력 |
| B7·B14 답글 작성   | ✅ 부모 댓글 + 기존 답글을 함께 렌더                           |
| B8·B15 댓글 수정   | ✅ 이미지 혼합 편집 · 뒤로가기 확인 모달                       |

**§4의 17개 차이 중 8건이 이 단계에서 갈렸다** — AppBar 제목(`민원 공간`) · 좋아요 라벨
(`동의해요`) · 처리상태 칩 · 수정·삭제 제한 · `space-y-2` 유무 ·
`BOARD_BLACK_LIST` 처리 누락 2건(#12는 댓글 삭제).

**공용 하부구조**: `convertFormDataFile` · `useUploadProgress`.

#### 재현한 레거시 기벽 5건

| 기벽                                             | 재현 방식                                               |
| ------------------------------------------------ | ------------------------------------------------------- |
| 🔴 **좋아요가 화면당 한 번만 시각 반응**         | mutation `isSuccess` 전이를 보는 effect (테스트로 고정) |
| 🔴 삭제·차단된 댓글의 **이미지는 계속 보인다**   | 이미지 렌더가 `state`를 보지 않는다                     |
| 🔴 **차단된 댓글에 답글 버튼이 보인다**          | 노출 조건에 `BLOCK`이 빠져 있다                         |
| 🔴 **익명 작성자의 차단 항목이 숨겨지지 않는다** | `DrawerList`의 `enabled`가 죽은 플래그 — 아래 참조      |
| 댓글 수정 성공 시 무효화·토스트 없음             | 뒤로가기만 한다 (`staleTime: 0`이 갱신을 대신한다)      |

#### `DrawerList`의 죽은 prop을 제거했다

레거시 조건이 `v-if="item.enabled ? item.enabled : true"`인데 **거짓일 때도 `true`가 되어**
어느 쪽이든 렌더된다 — `enabled`는 아무 일도 하지 않는다. Phase 4 이관본은 이를
"명시적 false면 숨김"으로 잘못 옮겨 놨었다. **레거시 동작(항상 렌더)에 맞추고 prop을 없앴다.**
아직 쓰는 곳이 없어 안전했고, 남겨두면 다음 도메인이 속는다.

#### 크래시는 재현하지 않았다

레거시 `DetailPostMoreButton`은 `postData?.authorText.split(...)`에서 `?.`가 `authorText`까지만
걸려 있어 **그 필드가 없으면 TypeError로 화면이 깨진다.** 옵셔널 체이닝을 이어 붙여
깨지지 않게 했다 — 정상 응답에서는 결과가 같고, 크래시를 옮길 이유가 없다.

### board 4/4 — 글 등록·수정 · 신고 · 미노출 사용자 ✅ **완료 (2026-07-31)**

**🎯 Board 도메인 완료. 21개 화면 전부 이관됐다.**

| 화면                   | 상태                                                   |
| ---------------------- | ------------------------------------------------------ |
| B9·B16 글 등록         | ✅ 카테고리 자동 드로어 · 수동 검증 모달 · 민원 비밀글 |
| B10·B17 글 수정        | ✅ 초기값 주입 · 카테고리 이름 역매칭                  |
| B20 게시글 신고        | ✅ 300자 JS 절단 · 라우터 state로 게시판 판별          |
| B19 미노출 사용자 관리 | ✅ 차단/해제 토글                                      |

#### 확정 2건을 여기서 적용했다

| #          | 적용 결과                                                                                                               |
| ---------- | ----------------------------------------------------------------------------------------------------------------------- |
| **BD-Q4**  | 폼이 **화면 스코프**다. 소통 → 민원 글쓰기로 값이 새지 않는다. 테스트로 `privateFlag`가 소통공간 페이로드에 없음을 고정 |
| **BD-Q11** | 수정 화면 초기값을 **데이터 도착 시** 주입한다. 쿼리 키에 `postUuid`가 들어가 캐시가 갈려도 폼이 비지 않는다            |

#### BD-Q12 결정 — 중첩 인터랙티브 요소는 옮기지 않았다

레거시 비밀글 체크박스는 `<button>` > `<label>` > `<input>` 중첩과 `preventDefault()`로
"꺼짐이면 모달만, 켜짐이면 즉시 해제"를 만든다. HTML 규격 위반이고 접근성 도구가 걸고
넘어지는 구조다. **동작만 재현하고 중첩은 옮기지 않았다** — 눈에 보이는 결과가 같다.
세 가지 클릭 시나리오를 테스트로 고정했다.

#### 🔴 명세 정정 1건 — 완료 버튼은 회색이 아니다

명세 §5-12는 "회색인데 눌리는 버튼"이라고 적었지만, 버튼 색을 정하는 vee-validate
`meta.valid`는 **에러가 비어 있으면 참**이고 수동 검증이 zod가 볼 세 필드를 먼저 막으므로
에러가 기록될 일이 없다. **실제로는 항상 브랜드 색이고 제출 중에만 잠긴다.**

#### 재현한 레거시 기벽

- 🔴 신고 화면은 **state가 없으면 민원공간 API로 간다**(새로고침·딥링크). 테스트로 고정
- 🔴 미노출 사용자 목록은 **3회째 토글부터 버튼이 멈춘다**(`isSuccess` 전이 의존)
- 신고는 `disabled`가 없어 **빈 내용으로도 제출된다**
- 해제해도 목록에서 사라지지 않는다

> ✅ **BD-Q11·BD-Q4 확정 (2026-07-31) — 4/4 착수 블로커 0건.**
>
> | #      | 결정                                                                                    | 화면에 미치는 영향                                                                   |
> | ------ | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
> | BD-Q11 | 상세 쿼리 키에 `postUuid`를 넣고 수정 폼 초기값을 **데이터 도착 시 `reset()`**으로 반영 | 정상 경로 동일. 레거시가 **빈 폼**을 띄우던 경우(캐시 미스·URL 직접 진입)만 채워진다 |
> | BD-Q4  | 글쓰기 폼을 **화면 스코프**에 둔다                                                      | 게시판 간 값 누수 제거. 대신 실수로 뒤로 가면 작성 내용을 잃는다                     |
>
> BD-Q11은 **버그 두 개가 서로를 가리고 있어** 한쪽만 고치면 수정 화면이 깨진다 —
> 반드시 함께 고친다 (`deferred.md` D-225·D-226).

### parking PR1 — 대시보드 · 마일리지 · 정기권 ✅ **완료 (2026-07-31)**

| 화면                           | 상태                                                                  |
| ------------------------------ | --------------------------------------------------------------------- |
| PK1 `/parking`                 | ✅ 잔여 마일리지 · 진행바 · 정책 드로어 · 메뉴 그리드 · 정기권 임베드 |
| PK2 `/parking/mileage/history` | ✅ 월 선택 · 카드 2개 · 무한 목록 · `미출차` 칩                       |
| PK15 `/parking/regular-car`    | ✅ 라우트이자 PK1 임베드. 월패드 칩                                   |

**도메인 공용 셸 `CardList`를 여기서 확정했다** — PR2·PR3의 4개 목록이 그대로 쓴다.

#### 🔴 마일리지 조회를 `shared`로 올렸다

메인 카드(`features/main`)와 주차 화면(PK1·PK2)이 **레거시에서도 같은 훅**
(`useGetParkingRemainingMileage`)을 썼다. feature끼리 import할 수 없으므로 공통분모를
`shared/lib/parkingMileage.ts` + `shared/hooks/useParkingRemainingMileage.ts`로 옮기고,
`features/main`의 것은 얇은 위임으로 바꿨다. **캐시 키도 하나로 합쳐진다**(레거시 동일).

| 호출부    | 기간      | 주차 구독 게이트                                              |
| --------- | --------- | ------------------------------------------------------------- |
| 메인 카드 | 이번 달   | ✅ 건다 (D-218). 카드 자체가 구독일 때만 렌더돼 결과 동일     |
| PK1       | 이번 달   | ❌ 안 건다 — URL 직접 진입이 가능해 레거시와 달라지면 안 된다 |
| PK2       | 선택한 달 | ❌ 〃                                                         |

#### 구조가 바뀐 곳 (화면 결과는 동일)

| 레거시                                                     | 이관본                                                                                                       |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `CardList`가 `useInfiniteScrollPosition`을 **조건부 호출** | 항상 호출하고 **`scrollRestorePath`가 있을 때만 ref를 붙인다** — ref가 없으면 effect가 바로 빠져나간다       |
| 목록 훅이 `setup` 본문에서 `removeQueries`                 | `useResetListCacheOnMount` — **첫 렌더 중** `useRef` 가드. `useEffect`로 옮기면 이전 캐시가 한 프레임 보인다 |
| `MileageCardMenus`가 `watch`로 기간을 받음                 | 기간을 prop으로 받아 그대로 조회 (초기값이 양쪽 다 이번 달)                                                  |
| 훅과 화면이 선택 월을 **따로** 보관                        | 훅이 하나로 소유 — 두 상태가 어긋날 여지가 사라진다                                                          |

#### 재현한 레거시 기벽

- 🔴 **정렬 파라미터가 서버에 닿지 않는다** — 훅은 `isLatest`를 보내고 API는 `isDesc`를 받는다. 테스트로 고정 (D-237)
- 🔴 **월패드 판정만 `trim()`을 하지 않는다** — 서버 값에 공백이 섞이면 칩이 통째로 사라진다. 테스트로 고정 (PK-Q1 · D-238)
- 🔴 **주차 구독 검사가 무효다** — `.value` 누락으로 항상 truthy. 주차 화면은 그대로 뒀다 (D-218)
- 로딩과 완료의 `gap`이 달라 전환 시 미세하게 흔들린다 (메뉴 그리드 · 마일리지 카드)
- 마일리지 에러 시 카드가 2개 → 1개로 줄고 제목도 바뀐다
- 정기권 카드가 `<button>`인데 클릭 핸들러가 없다
- 분이 0이면 `12시간 `처럼 뒤에 공백이 남는다

#### 고친 것 (재현할 가치가 없는 크래시)

`renderField`의 `mileagePolicy.minuteAmount.toLocaleString()`은 서버가 `mileagePolicy`를
빼면 화면을 깬다. 옵셔널 체이닝으로 막았다 — 정상 응답에서는 결과가 같다.

#### 결정 없이 지나간 것

**PK-Q5** — 마일리지 한도 제한 단지에서 `항상허용 차량` 메뉴는 사라지지만 **라우트(PK4)는
살려둔다.** 등가 이관의 기본값이고, 라우트 자체는 PR2에서 붙는다.

#### ⚠️ 이 단계에서 끝내지 못한 것

| 항목                       | 이유                                                           |
| -------------------------- | -------------------------------------------------------------- |
| **UI 픽셀 대조**           | 3화면. 그라데이션·진행바 타이밍·타일 흔들림을 나란히 봐야 한다 |
| **메뉴 3개가 404**         | 방문예약·입출차·즐겨찾기·항상허용은 PR2·PR3에서 붙는다         |
| **PK-Q2 (상세 복귀 감지)** | PK8·PK11에서만 필요하다 — **PR2 착수 전에 정해야 한다**        |

### parking PR2a — 차량관리 5화면 ✅ **완료 (2026-07-31)**

| 화면                                               | 상태                                                |
| -------------------------------------------------- | --------------------------------------------------- |
| PK3 `/parking/carManagement/bookmark/list`         | ✅ 별칭·연락처 · 별 아이콘 · 드로어 `수정`/`삭제`   |
| PK4 `/parking/carManagement/alwaysAllow/list`      | ✅ 연락처·메모 · 월패드 칩 · 드로어 `삭제`만        |
| PK5 `.../bookmark/add` · PK6 `.../alwaysAllow/add` | ✅ 필드 구성이 갈리는 한 컴포넌트 · 불러오기 드로어 |
| PK7 `.../bookmark/edit/:uuid`                      | ✅ 라우터 state로 초기값                            |

**항상허용 수정은 만들지 않았다** — 레거시에 라우트도 API도 없다 (R-1).

#### 🔴 실측으로 찾은 결함 1건 — 드로어가 화면마다 다르게 보인다

`CarManagementList`는 **어떤 목록을 보여줄지**(`isBookmarkList`)와 **어떤 필드를
보여줄지**(`carManagementType.key`)를 서로 다른 기준으로 정한다. 그래서 **항상허용
등록(PK6)에서 `즐겨찾기 차량 불러오기`를 열면 즐겨찾기 차량이 `연락처`·`메모` 필드로
그려지고 메모가 전부 `-`**다. 방문예약(PK12·PK13)에서 열면 경로에 `alwaysAllow`가 없어
`별칭`·`연락처`로 정상이다. 명세에 없던 항목이고 재현 + 테스트로 고정했다 (D-242).

#### 구조가 바뀐 곳 (화면 결과는 동일)

| 레거시                                        | 이관본                                                                                                                     |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| vee-validate `meta.valid`로 버튼 색           | **에러 개수**로 판정 — `meta.valid`는 "에러 없음"이라 첫 진입에 참이다. RHF `isValid`는 반대로 false에서 시작해 쓸 수 없다 |
| `InputRadioDual`의 `key`가 문자열·불리언 혼용 | 컴포넌트를 **제네릭**으로 바꿨다. 주차는 `true`/`false`가 그대로 서버에 간다                                               |
| 훅마다 복사된 `switch (errorCode)`            | `showCarMutationError({ handledCodes })` — **호출부가 코드 집합을 명시**해 훅별 차이를 유지                                |
| `memo` 초기값                                 | 기본값을 주지 않는다 — 빈 문자열을 넣으면 `memo: ""`가 서버에 간다                                                         |

#### 재현한 레거시 기벽

- 🔴 수정 폼 초기값이 `history.state`에만 있어 **새로고침하면 폼이 빈다** (D-243)
- 🔴 같은 에러코드가 **등록에서는 전용 문구, 수정에서는 서버 원문**이다 (D-245)
- 등록은 뒤로가기, 수정만 목록으로 이동 — 히스토리 스택이 달라진다 (D-244)
- 토스트 문구 3종이 제각각 (D-246)
- PK3에 들어가면 항상허용 캐시까지 지워진다 (두 훅이 항상 호출된다)

#### ⚠️ 이 단계에서 끝내지 못한 것

| 항목             | 이유                                                                                              |
| ---------------- | ------------------------------------------------------------------------------------------------- |
| **UI 픽셀 대조** | 5화면. 고정 버튼 2종(`bottom-0` vs `bottom-4`)을 봐야 한다                                        |
| **PK-Q8 확인**   | 방문목적 초기값이 레거시에서 왜 안 터지는지는 미확인이다 — 이관본은 빈 문자열로 안전하게 처리했다 |

### parking PR3 — 방문예약 4화면 ✅ **완료 (2026-07-31)**

**🎯 Parking 도메인 완료. 15개 화면 전부 이관됐다** (31파일 3,278 LOC).

| 화면                                  | 상태                                               |
| ------------------------------------- | -------------------------------------------------- |
| PK11 `/parking/reservation`           | ✅ 상태 칩 3종 · 월 보존 · 캐시/스크롤 복원        |
| PK12·PK13 `.../add` · `.../add/:uuid` | ✅ 한 컴포넌트. `uuid` 유무로 등록/재등록이 갈린다 |
| PK14 `.../detail/:uuid`               | ✅ 화면 내 AppBar(우측 `삭제`) · 메모만 HTML 렌더  |

#### 달력을 vue-datepicker → react-day-picker로 교체했다

**선택 규칙을 라이브러리에 맡기지 않았다.** `mode="range"`는 범위를 칠하는 용도로만 쓰고,
값은 `onDayClick`이 직접 계산한다 — 레거시 규칙이 라이브러리 기본 동작과 다르기 때문이다.

| 상황                      | 결과                            |
| ------------------------- | ------------------------------- |
| 시작일과 **같은 날** 클릭 | **무시**한다                    |
| 시작일보다 **이전** 클릭  | **교환**한다                    |
| 둘 다 있을 때 클릭        | 시작일만 남기고 종료일을 지운다 |

여섯 가지 시나리오를 테스트로 고정했다. 선택 범위(오늘~오늘+6)와 일요일 시작도 함께.

#### 🔴 타임존 보정을 그대로 옮겼다

`new Date(date.getTime() - offset).toISOString()` — 빼지 않으면 **자정 근처에 예약할 때
날짜가 하루 밀린다**(한국은 UTC+9라 09:00 이전이 전날이 된다). `23:30`으로 시계를 고정한
테스트로 고정했다.

#### 🔴 크래시 1건을 고쳤다 (PK-Q10)

재등록은 상세가 주는 **방문목적 이름**으로 목록에서 uuid를 되찾는데, 레거시는
`.find(...).uuid`에 옵셔널이 없다. **관리사무소가 그 방문목적을 지우면 화면이 통째로
깨진다.** 이관본은 못 찾으면 비워 두고 사용자가 다시 고르게 한다 (D-256).

#### ✅ PK-Q11 오타 수정을 적용했다

예약 **삭제** 실패의 `case 'RESERVATION_DATE_INVALID('`(괄호) 때문에 서버 원문이 뜨던 것을
`예약일자는 7일 이내로 선택가능합니다.`로 고쳤다. 등록과 삭제의 **문구 표가 다르다**는 점도
함께 옮겼다(같은 코드, 다른 문구).

#### ⚠️ 이 단계에서 끝내지 못한 것

| 항목             | 이유                                                                     |
| ---------------- | ------------------------------------------------------------------------ |
| **UI 픽셀 대조** | 4화면. 특히 달력은 라이브러리가 바뀌어 **모양이 가장 많이 다를 수 있다** |
| **번들**         | react-day-picker + date-fns 로케일로 주차 청크가 51 → 149 kB (D-260)     |

### parking PR2b — 입출차·거부 3화면 ✅ **완료 (2026-07-31)**

| 화면                                     | 상태                                                       |
| ---------------------------------------- | ---------------------------------------------------------- |
| PK8 `/parking/inoutHistory`              | ✅ 월 선택 보존 · 유형 칩 8종 · 캐시/스크롤 복원           |
| PK9 `/parking/inoutHistory/detail/:uuid` | ✅ 7필드 · 거부 영역 · 입출차 사진. **푸시 딥링크 도착지** |
| PK10 `/parking/reject/:uuid`             | ✅ 100자 초과 입력 허용 + 제출 시 검증                     |

#### PK-Q2(A안) 구현 — 상세 복귀 감지

레거시 `history.state.forward`를 대신해 **상세로 나가는 순간 화면이 표시를 남기고**
(`markLeavingToDetail`), 언마운트 시 목록별 키로 저장한 뒤 다시 마운트될 때 읽는다.
**`POP`일 때만 인정**해 "상세에서 앞으로 더 이동한 뒤 새로 목록에 들어오는" 경우를 거른다.

| 진입 경로     | 캐시            | `staleTime` |
| ------------- | --------------- | ----------- |
| 상세에서 복귀 | **비우지 않음** | `Infinity`  |
| 그 외         | 비운다          | 기본값(0)   |

> 🔴 **처음 구현은 언마운트 시 `window.location.pathname`을 읽었는데 잘못이었다.**
> `MemoryRouter`에서는 그 값이 라우터 경로가 아니라 jsdom URL이라 **테스트로 검증할 수
> 없는 코드**가 된다. 게시판 B1에서 같은 문제를 겪고 쓴 방식으로 바꿨다.
> 상세 왕복 시 요청이 1회로 유지되는 것을 테스트로 고정했다.

#### 그 밖에 바뀐 것

| 항목                    | 처리                                                                       |
| ----------------------- | -------------------------------------------------------------------------- |
| 선택한 달 보존          | `history.replaceState` → **라우터 state**(`navigate(replace)`)             |
| `ChipBase.color`        | **옵셔널로 완화.** 표에 없는 차량 유형이 오면 레거시가 색 없는 칩을 그린다 |
| 거부 영역의 `<ul>>div>` | `<li>`로 감싸 HTML 규격 위반만 고쳤다 — 보이는 결과는 같다                 |

#### 재현한 레거시 기벽

- 🔴 차량 거부의 차량번호가 라우터 state에만 있어 **새로고침하면 빈 값으로 전송된다** (D-251)
- 🔴 총 주차시간 0을 **목록은 `0분`, 상세는 `-`**로 쓴다 (D-249)
- 사진이 없는 칸만 비율이 2:1 → 16:9로 바뀐다 (D-250)
- 거부 실패 문구의 띄어쓰기 오류 (D-252)
- 거부 사유는 100자를 **넘겨 입력할 수 있고** 제출할 때 막힌다 (신고 화면은 잘라낸다)

---

## 다음 작업

**Phase 0~~5 완료. Phase 6 진행 중 (auth A1~~A4 완료).**

### A. 착수 전 사용자 결정 — ✅ **3건 전부 확정 (2026-07-30)**

| #     | 항목            | 결정                                                                                                                                                                                                                                                                                                                                           |
| ----- | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **의존성 승인** | ✅ **8개 승인** — `react-day-picker` · `dompurify` · `swiper` · `qrcode` · `html2canvas` · `quill-delta-to-html` · `he` · `posthog-js`<br>🚫 **`@sentry/vite-plugin` 제외** ("일단 sentry는 빼고") · 🚫 `lodash-es` 불필요                                                                                                                     |
| **2** | **`MF-Q1`**     | ✅ **관리비 목업 화면도 이관한다.** ApexCharts 2개 → recharts. 목업 데이터·`TODO` 주석 유지, 진입 경로는 만들지 않는다                                                                                                                                                                                                                         |
| **3** | **`B-Q2`**      | ✅ **미생성 클래스를 현 Tailwind 토큰으로 표현.** `globalColor.scss` 팔레트를 되살리지 않고 `@theme`에 새 색을 추가하지 않는다.<br>🔴 **2026-07-30 정정** — 이 결정은 "어떤 토큰으로 표현할지"였고 **화면을 바꾸라는 것이 아니었다.** 죽은 클래스는 **렌더값**(text `#111927` · border `#E5E7EB` · bg 없음)으로 옮긴다 → `broken-styles.md` §0 |

> 🎯 **Phase 4 착수 블로커 0건.**

#### `@sentry/vite-plugin` 제외의 범위

| 항목                                         | 처리                                                 |
| -------------------------------------------- | ---------------------------------------------------- |
| `sentryVitePlugin` · 소스맵 업로드·삭제      | 🚫 **하지 않는다** (`build.sourcemap`도 끈다)        |
| `SENTRY_AUTH_TOKEN` Secret                   | 🚫 불필요 (Phase 7 이전 목록에서 제외)               |
| **`@sentry/react`** (타깃 기설치)            | ✅ **유지** — `main.tsx`·`QueryErrorBoundary` 그대로 |
| `sentryApiError.js` 이식 · `VITE_SENTRY_DSN` | ✅ 유지                                              |

**즉 에러 리포팅은 살아 있고 스택트레이스만 난독화 상태로 남는다.**
Sentry 통합 자체를 걷어내려면 `env` 스키마·`main.tsx`·`QueryErrorBoundary`·`sentryApiError`
4곳을 함께 정리해야 한다 — **그 결정은 아직 받지 않았다.**

#### 미생성 클래스 조치 (`broken-styles.md` §0)

🔴 **2026-07-30 정정.** 이전에는 아래 표의 "확정 토큰"을 적용해 **화면이 달라지는 것**을
의도된 변화로 적어뒀다. 그것은 결정을 잘못 기록한 것이다 — 사용자 지시는
*"화면은 그대로여야지"*였다. **이관 시에는 레거시가 실제로 렌더한 값을 쓴다.**

| 깨진 클래스                                                            | 레거시 렌더값    | 이관 시 쓸 것               |
| ---------------------------------------------------------------------- | ---------------- | --------------------------- |
| `text-brand-primary-50` · `-100`                                       | 상속색 `#111927` | `text-neutral-b-gray-900`   |
| `text-neutral-90`                                                      | 〃               | 〃 (값이 같아 그대로)       |
| `text-neutral-70` · `-40`                                              | 〃               | 〃                          |
| `border-neutral-20` · `-10` · `border-deep-glue-20` · `border-bg-gray` | 기본 `#E5E7EB`   | `border-neutral-b-gray-200` |
| `bg-neutral-10` · `bg-brand-...-brand-secondary`                       | 배경 없음        | 클래스 제거                 |

> 디자이너 의도 복원(파랑 제목·회색 placeholder·연파랑 배경)은 **전환 후 별도 작업**이다.
> 예외는 `SignUpAptInfoRadio` 하나다 — ✅ **2026-07-30 확정: `bg-primary-pc-indigo-400`
> (`#7F98F9`)으로 복원한다.** 렌더값을 그대로 옮기면 선택된 칩이 흰 글자만 남아 보이지 않는다.
> **이 자리에 한정된 예외이며 나머지 25건은 렌더값 규칙 그대로다** (`broken-styles.md` §0).

### B. 공용 인프라 ↔ 소비 도메인 (Phase 4에서 대부분 해소)

| 공용물                              | 소비 도메인                                                   | 상태                                                              |
| ----------------------------------- | ------------------------------------------------------------- | ----------------------------------------------------------------- |
| **날짜 선택기 래퍼**                | AptMall(AM9, `inline`) · 주차 · **이사예약**(휴무일) · 로비폰 | ⏳ `shadcn calendar` 설치만. 래퍼는 AptMall AM9 기준 (MH-Q12)     |
| **`ImageUploader` + 진행률**        | **Board**(10~11곳) → Repair                                   | ⏳ Board 이관과 함께                                              |
| **`CanvasSign`**                    | Vote(모달) · **소방**(화면 단계)                              | ✅ 이식 완료                                                      |
| **`DrawerMonth`**                   | 관리비 상세 · **메인 카드**                                   | ✅ 이식 완료 (선택 값을 부모가 소유하게 바꿔 D-143도 함께 해소)   |
| **`usePermissionInfo`**             | A-PASS · **메인 배지**                                        | ✅ `subscribeToPermissionInfo` + `useNativeSubscription`으로 가능 |
| **네이티브 브릿지 24종**            | **Visit(7화면) · A-PASS** — 이 둘은 브릿지 없이 이관 불가     | ✅ 24종 전부 이식                                                 |
| **`InputRadioList`** (폼 결합 제거) | 소방 · 이사예약 · 하자보수                                    | ✅ 제어 컴포넌트로 통일                                           |
| **오버레이/에러 모달**              | 69개 파일 · 호출 293곳                                        | ✅ `showErrorModal()`                                             |
| **`useInfiniteList`**               | 목록 18종                                                     | ✅ 이식 완료                                                      |

### C. Phase 6 이관 순서 제약 (명세에서 확정된 것)

```
Phase 4 브릿지 완료 ─┬─→ Visit
                     └─→ A-PASS (+ Main 배지와 같은 PR)

Board ──→ Repair            (ImageUploader·useUploadProgress·convertFormDataFile·validImage)
Vote ──→ Survey             (opinion 엔트리 + KMC 인프라)
Main ──→ 관리비 상세(MF1)    (DrawerMonth)
관리비 MF2 ──→ Main 카드      (recharts 규격을 옵션이 복잡한 MF2에서 먼저 확립)
AptMall ─→ 주차/이사예약/로비폰 (날짜 선택기 래퍼를 AM9 기준으로 먼저 만든다)

브릿지 독립 (아무 때나): AptMall · 소방 · 이사예약 · 하자보수 · 관리비
```

**opinion 앱은 마지막에 몰지 않는다** — 골격을 Phase 4에 세우고 Vote·Survey 이관 때 각자 배선한다
(계획서 리스크 R9 · `opinion.md` 「이관 순서」).

### D. 확인 항목 현황

**도메인 명세 17개에 확인 항목 총 164건**(2026-07-30 기준 **해소 16건**)이 쌓였다.
전부 진행을 막지는 않지만, **이관 착수 전에 도메인별로 한 번 훑어야 한다.**

| 성격                           | 예                                                                             | 처리 시점           |
| ------------------------------ | ------------------------------------------------------------------------------ | ------------------- |
| **버그 수정 여부** (등가 충돌) | `RP-Q4`(무한스크롤 미동작) · `AP-Q3`(뒤로가기 영구 차단) · `O-Q7`(토스트 무음) | **도메인 착수 전**  |
| **서버 응답 실물 확인**        | `AM-Q10`(단가/줄합계) · `F-Q7`(정렬 보장) · `RP-Q8`(필드명)                    | 백엔드 문의         |
| ~~**디자인 확인**~~            | ~~`B-Q2` · `F-Q11` · `MH-Q11` · `RP-Q10`~~                                     | ✅ **A-3에서 확정** |
| **스타일 오타 수정**           | `broken-styles.md` **26건 전부 조치 방침 확정** (미해결 0)                     | 도메인 이관 시      |
| **개선 아이디어**              | `deferred.md` **D-1~D-183**                                                    | 전환 후             |

### E. 전환 후 별도 작업 — 우선순위 있는 것

> **이관 중에는 손대지 않기로 확정된 것들.** 정본은 `deferred.md`이고,
> 여기에는 **먼저 볼 것**만 추린다.

| 항목                                 | 내용                                                                                                   | 결정일     |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------ | ---------- |
| **D-219 메인 진입 번들 507 kB**      | recharts 90 kB(gzip) + swiper 29 kB. **도넛을 SVG로 직접 그리는 것부터** — 회수량 3배에 위험은 더 낮다 | 2026-07-31 |
| D-15·D-16·D-17 인증 보안 3건         | 평문 자격 저장 · 비밀번호 쿼리스트링 · 쿠키 refresh 전환. 백엔드 협의 + 웹뷰 쿠키 실기기 검증이 선행   | 2026-07-29 |
| D-39 토큰을 URL로 넘기는 외부 링크   | 커뮤니티V2 · 쇼핑몰. 히스토리·리퍼러·서버 로그에 토큰이 남는다                                         | 2026-07-30 |
| D-9·D-49·D-50·D-51 서버·앱 계약 오타 | 고치면 기능이 깨진다. **앱·백엔드와 함께** 수정해야 한다                                               | 2026-07-29 |

---

## 세션 로그

| 날짜       | 작업                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-29 | 계획 승인. 기준 SHA 고정(`6d5bf22`). `feat/migration-inventory` 브랜치 생성                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-29 | `routes.md` 완료 — 화면 라우트 121개 전수                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-07-29 | `endpoints.md` 완료 — 엔드포인트 148개 전수                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-29 | 확인 항목 11건 전부 확정. `decisions/inventory-questions.md`, `deferred.md` 작성                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-29 | **인증 방침 변경** — 레거시 방식 유지로 확정. `decisions/auth-strategy.md` 작성. 계획서 반영(Phase 0 블로커 3→1, R3·R4·R11 소멸, R12·R13 추가)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-29 | `native-protocol.md` 완료 — 브릿지 24종 전수. **0-4 해소**(웹이 레거시 프로토콜에 맞춤). 외부 블로커 0건                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-29 | `query-keys.md`·`domain-codes.md`·`env-vars.md` 완료. **Phase 1 종료**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-29 | 기술 선택 4건 확정 (0-5·0-6·에러 모달·QueryClient 기본값). `decisions/tech-choices.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-29 | `zod-migration.md` 완료 (0-7). **Phase 0 종료**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-29 | `tech-mapping.md` 완료. **Phase 3 종료**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-29 | `features/auth.md`(Intro+Login) · `features/exception.md` 완료. Phase 2 착수                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-29 | `features/signup.md` 완료 (SignUp 8 + TermsOfUse 3). 확인 항목 S-Q1 코드로 확정                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-29 | `features/main.md` 완료 (22파일). 콘텐츠 플래그 4개 미사용 확인, M-Q3 확정                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-29 | `features/mypage.md` 완료. **기반 도메인 6개 전부 완료.** P-Q3·P-Q4 코드로 확정                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-29 | `broken-styles.md` 작성 — Tailwind 빌드로 미생성 클래스 16개 확정. 12개 수정, 4개는 디자인 확인 대기                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-29 | `features/board.md` 완료 (48파일 · 화면 20 + 팝업 1 · 2,984줄). 결함 15건·소통↔민원 차이 17건 정리. 확인 항목 BD-Q1~Q15                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-29 | **`broken-styles.md` 2차 조사 — 미생성 클래스 16 → 25로 정정.** 토큰 821개 전수 빌드 검증                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-07-29 | `features/parking.md` 완료 (31파일 · 화면 15 · 2,221줄). 미출차 일체 제외 확정 반영. 확인 항목 PK-Q1~Q11                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-29 | `features/visit.md` 완료 (23파일 · 화면 13 · 1,463줄). 브릿지 6종 의존. 확인 항목 V-Q1~Q9. PK-Q6 해소                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-29 | `features/vote.md` 완료 (23파일 · 라우트 13 = 메인 6 + opinion 7 · 1,293줄). 확인 항목 VT-Q1~Q9. 미생성 클래스 `center` 추가 발견(25→26)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-29 | `features/survey.md` 완료 (19파일 · 라우트 14 = 메인 6 + opinion 8 · 1,053줄). **Vote와의 차이 9건** 정리. 확인 항목 SV-Q1~Q10. SV-Q5 해소                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-30 | `features/apt-mall.md` 완료 (19파일 · 라우트 3 + 드로어 위저드 8 = 화면 11 · 1,584줄). **브릿지 의존 0.** 결함 13건. 확인 항목 AM-Q1~Q22. E-Q6 전제 정정                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-30 | `features/fire-inspection.md` 완료 (13파일 · 라우트 4 + 단계 2 · 1,350줄). 점검표 21항목이 클라이언트 하드코딩. `lodash` 제거 가능 확인. 확인 항목 F-Q1~Q16                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-30 | `features/moving-house.md` 완료 (10파일 · 라우트 4 · 1,265줄). `chargeFlag`가 6곳을 동시에 바꾼다. 신축 입주 기간 로직. 달력 5개 전수 비교. 확인 항목 MH-Q1~Q15                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-30 | `features/repair.md` 완료 (11파일 · 라우트 4 · 1,080줄). Pinia 안의 vee-validate `useForm`. 결함 밀도 최고 — 무한 스크롤 미동작·AppBar 이중·`'write'`/`'create'` 불일치. 확인 항목 RP-Q1~Q12                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-30 | `features/management-fee.md` 완료 (3파일 · 라우트 2 · 765줄). 🔴 **`/managementFee/info`(524줄)가 도달 불가 + 전부 목업** → 이관 제외 권장(`MF-Q1`). `startDateTIme` 오타 유지 재확인. 확인 항목 MF-Q1~Q9                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-07-30 | `features/apass.md` 완료 (3파일 · 라우트 1 · 620줄). 브릿지 4종 왕복. 🔴 `invalidateQueries` v4가 **UI 갱신의 유일한 경로**. 7초 타임아웃이 전역 로딩 플래그를 못 내려 뒤로가기 영구 차단. 확인 항목 AP-Q1~Q6                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-07-30 | `features/opinion.md` 완료 (엔트리·빌드·레이아웃·라우터 · 765줄). 🔴 **`LayoutOpinionBase` 2중 중첩** — `pt-6`×2=48px에 의존, `ToastContainer`가 버려져 **모든 토스트 무음**. `O-Q4`·`O-Q10` 실측으로 해소. **Phase 2 종료 (19/19)**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-30 | 🎯 **`broken-styles.md` §5 2건 해소** — `globalColor.scss`(이전 SCSS 팔레트)에서 `border-deep-glue-20`→`$deep-blue-20 #e6e6ec`, `border-bg-gray`→`$bg-gray #f8f8f8` 확인. Tailwind config 이식 시 `deep-blue`·`bg-*` 계열이 누락된 것                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-30 | ✅ **사용자 결정 3건 확정** — ① 의존성 8개 승인 · `@sentry/vite-plugin` 제외 ② 관리비 목업 화면(MF2) **이관** — §1-2에 recharts 대조표 포함 전수 명세 작성 ③ 미생성 클래스 6건을 **현 Tailwind 토큰으로 매핑**. **Phase 4 착수 블로커 0건**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-30 | **Phase 4 1단계** — 의존성 8개 설치(+shadcn 12종) · `env.ts`를 공통/메인전용 스키마로 분할해 **V-Q2 해소** · `vite.config.ts test.env` · `FEATURE_SLICES` 18개 eslint zone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-30 | **Phase 4 2단계** — 디자인 토큰 이식(색 207 파싱 생성 · 타이포 67 `@utility` · `--font-scale` · `sm:392px` · `public/assets` 210개). 🔴 **Tailwind v3→v4 클래스 변경 4종 55곳 실측** — `rounded`(bare) 46곳이 v4에서 무시된다(§10-1). 죽은 코드 6건 이식 제외(D-184~D-190)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-30 | **Phase 4 3단계** — HTTP 레이어 재작성. 재발급 트리거를 401 → **errorCode**로, 토큰을 메모리 → **localStorage**(레거시 키·따옴표 직렬화 유지), 대기 큐 + 자동 로그인. 타임아웃·paramsSerializer 제거. 테스트 19개. D-191~D-196                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-30 | **Phase 4 4단계** — 네이티브 브릿지 재작성. `JsInterface` · `{type,data}` · iOS 객체/Android 문자열 · `window.CALLBACK_*` 7종 · mitt 자체 구현. zod 필드를 **전부 optional**로(필드 누락 메시지를 버리지 않게). 푸시 딥링크를 큐 + 이벤트로(순환 의존 제거). 테스트 14개                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-30 | **Phase 4 7단계** — `showErrorModal()`로 `swalErrorModal`(293곳) 대체. SweetAlert2 v11 기본 수치 재현. `ModalBase`·`DrawerBase`. 테스트 7개                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-30 | **Phase 4 5·6·9·11단계** — 인증 슬라이스(에러코드 분기·`loginDataHandler`·로그아웃 플로우) · `aptInfo`를 `authStore`에 배치 · 레이아웃 셸(route `handle` 메타·뒤로가기 라우팅표·앱 종료 모달) · `features/dashboard` 삭제. **`AuthProvider`를 라우터 안으로 옮겼다**(밖에 있으면 `useNavigate`가 죽는다)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-30 | **Phase 4 10단계** — `useInfiniteList` 이식(페이지 크기 10 · `pages[0]` pageable · 접두사 `resetCache` · `Object.values` 키 조립 유지). 테스트 6개                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-30 | **Phase 4 8단계** — 공용 컴포넌트 38개 완료. common 30개 재작성 + 레이아웃 5개 + Toast 2개 sonner 대체. 제어 컴포넌트로 바꾼 3곳에서 `useEffect` 안티패턴과 D-143을 함께 제거                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-07-30 | **Phase 4 완료 기록** · env 빈 값 함정 수정(`optionalEnv`) · 검증 실패 로그를 `prettifyError`로 · **`VITE_ENV` 제거** → Vite `MODE`에서 `env.APP_ENV` 파생(사용자 지적). V-Q3 소멸                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-07-30 | **Phase 5-1** — 단지 컨텍스트를 `shared`로 승격. `useResidentDetailInfo`(사용처 23파일) · `aptContext` · `useChangeApt` · `useLogoutFlow` 이동 · `APT_CONTENT_NAME` 17종. `getResidentAptList`·`hasAptService` 중복 제거                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-30 | **Phase 5-2** — `features/exception` E1·E2·E3 이관. 단지 컨텍스트 조회 실패의 이동 대상(`/error-auth`)이 없어서 먼저 만들었다. 템플릿 404 교체 · `isLoggedIn()`(토큰만 보는 판정) 추가                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-30 | **Phase 5-3 (파일럿 완료)** — MyPage 8화면 + `/logout` 이관. 🔴 **동의 토스트 stale closure 버그를 테스트가 잡았다**(Vue `ref` → React 클로저). RHF는 `Controller`로 제어 컴포넌트에 연결, 버튼 활성은 `errors` 비어 있음으로 재현. 의사요소 CSS를 `index.css`로. 테스트 29개(총 83). **`recipe.md` 작성**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-30 | **Phase 5 정정 2건** — ① 죽은 클래스를 "이름이 암시하는 색"으로 바꿨던 것을 **레거시 렌더값**으로 되돌림(11곳). `broken-styles.md` §0 신규 · 사용자 지적 ② 🔴 **`PageTransition`이 높이 없는 래퍼를 끼워 전 화면 스크롤이 죽어 있었다** — Vue `<Transition>`은 DOM 노드를 안 만든다. `h-full` 추가로 수정, `e2e/layout.spec.ts`(5개)로 회귀 방어. 템플릿 dashboard e2e 삭제(CI 적색이었다)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-30 | 🎯 **Phase 4 종료.** 완료 조건 검증 — `router.test.tsx`가 실제 라우트 트리로 로그인→메인 사슬 전체 통과. 4개 검증 통과 · 테스트 **51개**. ⚠️ `.env.*`는 권한 차단으로 사용자가 직접 추가해야 한다(`env-vars.md` §7)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-30 | **Phase 6 착수 — auth A1~A4 이관** (`40659c4`). 인트로를 레거시 마크업으로 교체(템플릿 카드 폐기) · 비밀번호 재설정 2화면 · 승인 대기. **라우터 가드 완성**(`publicRouteLoader`는 컴포넌트가 아니라 loader — 인트로의 `clearAuth()`가 먼저 돌면 세션이 지워진다 · `useBlocker`로 오프라인·뒤로가기 차단). `renderWithProviders`에 `initialEntries` 추가. D-205~D-210 기록. 테스트 106개 통과                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-30 | **SignUp + auth A5·A6 이관.** S1~~S5 · 약관 상세(`features/terms`) · 버전1 전환 2화면. KMC 본인인증(`CertButton` — 외부 폼 POST)과 약관 동의 폼을 `shared`로 올렸다(4도메인·2도메인 공유). `useTermsAgreement`가 `watch` 3개를 파생상태 1 + 이벤트핸들러 2로 나눈 첫 사례. **실측 정정 2건**: `SignUpAptInfoRadio.vue`는 죽은 파일(B-Q1 적용 자리 없음) · S3는 AppBar가 2개 겹친다. D-211~~D-215 기록. 테스트 120개 통과                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-07-30 | **Main 1/3 이관** — 골격·권한요청·비컨 우회·단지 헤더·단지 전환 드로어. **`useChangeApt`의 경쟁 조건을 해소했다**(`fetchQuery` 응답에서 직접 읽음 — 레거시는 `contentList: []`를 앱에 보낼 수 있었다). 정정 1건: `승인되지 않은 단지입니다.` 모달은 레거시에서 뜨지 않는다(D-216). `ResidentApt`에 `dong`·`ho`·`aptAddress` 추가. nbsp가 접근성 이름을 깨는 테스트 함정을 recipe에 기록. 테스트 127개 통과                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-30 | **Main 2/3 이관** — 카드 그리드(`useMainCardLayout` 242 LOC) + 카드 5종 + A-PAY 2카드. **개수별 배치 프리셋을 테스트 8개로 고정**했다(1·2·3·4·4특수·5장 + 빈 상태 + 로비폰 OR). ApexCharts radialBar → recharts `RadialBarChart`(각도 기준이 달라 `90 → -270`). 레거시 버그 2건은 고쳐 옮겼다 — 마일리지 쿼리 키에 uuid 없음(D-217) · 주차 구독 검사 `.value` 누락(D-218). recharts 때문에 메인 청크 310 kB(D-219). 테스트 135개 통과                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-31 | **Main 3/3 이관 — Main 도메인 완료.** 메뉴 스와이퍼(swiper/react) · 광고 배너 · 공지 Top3 · 쇼핑 마케팅 동의 바텀시트. **메뉴 노출 규칙을 테스트 7개로 고정**했다 — 특히 스와이퍼가 `'투표'`, 카드 게이트가 `'전자투표'`를 보는 레거시 불일치(M-Q3)를 못박았다. `lodash` 대신 `chunk`를 직접 만들었다(3줄). 죽은 코드 3건 미이관(`MainNotOutHistory` 주석 처리 · `FIXED_MENUS`의 `'공지사항'` · `recentlyNotice`). D-221·D-222 기록. **swiper가 더해져 메인 청크 310 → 483 kB**(D-219 갱신). 남은 것은 공지·투표 팝업 2건뿐이며 Board·Vote 이관 후에 붙인다. 테스트 163개 통과                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-31 | **Main 도메인 확인 항목 정리.** M-Q4 확정(임시 배너가 의도된 것이 맞다 → D-40 닫음). M-Q1·M-Q5는 **확인 필요·현행 유지**로 남긴다. M-Q2는 참고용(이관본은 이미 고쳐져 있어 답과 무관). **D-219 번들을 `manualChunks`로 실측** — recharts 90 kB·swiper 29 kB(gzip), 우리 코드는 44 kB다. recharts가 swiper의 3배라 도넛 SVG화가 먼저다. **전부 현행 유지하고 전환 후 별도 작업으로 뺀다**(§E)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-07-31 | **Board 1/4 이관 — 공지 계보** (B1~B4 + B21 공지 팝업). 공용 하부구조 5개를 함께 만들었다(`convertDeltaToHtml` · Quill CSS 937줄 · `formatIsoStringDate` · `useIntersectionObserver` · `useInfiniteScrollPosition`). **메인의 공지 팝업이 해결됐다** — 팝업이 Board 소유라 `app/MainScreen`에서 조립한다(feature 간 import 금지). **BD-Q3 결정**: 글자별 `<span>`은 재현하지 않는다(렌더 동일 + 스크린리더 악화). 🔴 실측 결함 1건: `convertDeltaToHtml`이 배열 Delta에서 **반드시 실패**해 `정보없음`이 뜬다(D-223) — 재현했다. 배럴 때문에 게시판 청크가 메인과 함께 로드된다(D-224). 테스트 193개 통과                                                                                                                                                                                                                                                                                                                                                |
| 2026-07-31 | **Board 2/4 이관 — 소통공간·민원공간 목록 + 내 활동** (B5·B12·B11·B18). 두 게시판은 이 화면에서 라벨·경로만 다르므로 한 컴포넌트로 합쳤다. 🔴 **내가 만든 버그 1건을 잡았다** — `useIntersectionObserver`가 `useRef` + 빈 deps라 **센티널이 나중에 생기는 화면에서 무한스크롤이 아예 동작하지 않았다**(Board 1/4의 B1 포함). 콜백 ref로 고쳤다. `useKoreanTimeAgo`는 임계값을 짐작하지 않고 **레거시의 실제 `@vueuse/core`를 실행해 18개 값을 뽑아** 테스트 기준표로 넣었다(45분 59초 → `46분 전` 반올림까지 일치). jsdom에 없는 `IntersectionObserver` 스텁을 테스트 셋업에 추가. 테스트 225개 통과                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-31 | **Board 3/4 이관 — 상세 + 댓글** (B6·B13·B7·B8·B14·B15). 도메인 최대 덩어리이고 §4의 17개 차이 중 8건이 여기서 갈렸다. **레거시 기벽 5건을 재현**했다 — 좋아요가 화면당 한 번만 반응(D-228) · 삭제된 댓글의 이미지 잔존(D-229) · 차단 댓글의 답글 버튼(D-230) · 익명 작성자 차단 항목 미숨김(D-231) · 댓글 수정 후 무효화·토스트 없음. **`DrawerList`의 죽은 `enabled` prop을 제거**했다(Phase 4 이관본이 레거시와 반대로 동작하고 있었다). 크래시 1건(D-232)은 재현하지 않고 고쳤다. BD-Q11 확정에 따라 상세·댓글 쿼리 키에 `postUuid`를 넣었다. 테스트 253개 통과                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-31 | **Board 4/4 이관 — 🎯 Board 도메인 완료** (B9·B10·B16·B17 글 폼 · B20 신고 · B19 미노출 사용자). **21개 화면 전부 이관됐다.** 확정 2건을 적용했다 — BD-Q4(폼 화면 스코프, 게시판 간 값 누수 제거) · BD-Q11(초기값을 데이터 도착 시 주입). **BD-Q12 결정**: 비밀글의 `button>label>input` 중첩은 옮기지 않고 동작만 재현했다(HTML 규격 위반이고 렌더 결과가 같다). 🔴 **명세 정정**: §5-12의 "회색인데 눌리는 완료 버튼"은 틀렸다 — `meta.valid`가 계속 참이라 **항상 브랜드 색**이다. 레거시 기벽 재현: 신고 새로고침 시 민원 API(D-233) · 미노출 목록 3회째 토글 멈춤(D-234) · 빈 내용 신고 가능(D-236). 폼 미리보기 objectURL 누수(D-235)는 고쳤다. 테스트 271개 통과                                                                                                                                                                                                                                                                                  |
| 2026-07-31 | **커밋 메시지 형식 변경.** 히스토리 45개의 `type(scope):`를 `type : (scope)`로 재작성했다(미push 범위만, 내용·시각 보존). `15-git.md`가 이 형식을 금지하고 있어 함께 고쳤다 — **범위는 되도록 쓰지 않고**, 쓸 때만 요약 앞에 괄호로 둔다. ⚠️ 콜론 앞 공백 때문에 Conventional Commits 규격이 아니다(commitlint 도입 시 걸린다)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-31 | **Parking PR1 이관 — 대시보드·마일리지·정기권** (PK1·PK2·PK15). 도메인 공용 목록 셸 `CardList`를 확정했다(PR2·PR3의 4개 목록이 쓴다). 🔴 **마일리지 조회를 `shared`로 올렸다** — 메인 카드와 주차 화면이 레거시에서도 같은 훅을 썼는데 feature끼리 import할 수 없다. 주차 구독 게이트는 메인에만 남겼다(주차 화면은 URL 직접 진입이 가능해 레거시와 달라지면 안 된다). 구조 변경 4건은 화면 결과가 같다 — 조건부 컴포저블 호출 → ref 부착 여부로, `removeQueries` → 첫 렌더 `useRef` 가드(effect로 옮기면 이전 캐시가 한 프레임 보인다), `watch` 기간 동기화 → prop, 선택 월을 훅이 단독 소유. 레거시 기벽 재현: 정렬 파라미터 미전송(D-237) · 월패드만 `trim()` 안 함(D-238) · 주차 구독 검사 무효(D-218). 크래시 1건은 옵셔널 체이닝으로 고쳤다. **PK-Q5는 등가 기본값으로 처리**(항상허용 라우트 유지). 테스트 303개 통과, parking 청크 20.4 kB / gzip 6.0 kB                                                                                         |
| 2026-07-31 | **Parking PR2a 이관 — 차량관리 5화면** (PK3·PK4 목록 · PK5·PK6·PK7 폼). 🔴 **명세에 없던 결함을 찾았다** — `CarManagementList`가 "어떤 목록"과 "어떤 필드"를 서로 다른 기준으로 정해서, **항상허용 등록에서 `즐겨찾기 차량 불러오기`를 열면 즐겨찾기 차량이 항상허용 필드로 그려지고 메모가 전부 `-`**다(D-242). 재현 + 테스트로 고정. 구조 변경 4건은 화면 결과가 같다 — 버튼 색을 `meta.valid` 대신 **에러 개수**로(RHF `isValid`는 false에서 시작한다), `InputRadioDual`을 **제네릭**으로(주차는 `true`/`false`를 값으로 쓴다), 훅마다 복사된 `switch (errorCode)`를 **호출부가 코드 집합을 명시**하는 헬퍼로, `memo` 기본값 제거(빈 문자열을 넣으면 `memo: ""`가 서버에 간다). 레거시 기벽 재현: 수정 폼이 `history.state`에만 의존해 새로고침 시 빈다(D-243) · 같은 에러코드가 등록/수정에서 다른 문구(D-245) · 등록은 뒤로, 수정만 목록으로(D-244). **항상허용 수정은 만들지 않았다**(R-1). 테스트 326개 통과, parking 청크 39.5 kB / gzip 10.4 kB |
| 2026-07-31 | **Parking PR2b 이관 — 입출차·거부 3화면** (PK8 목록 · PK9 상세 · PK10 거부). **PK-Q2를 A안으로 구현**했다 — 상세로 나갈 때 화면이 표시를 남기고, 목록별 키로 저장했다가 `POP`으로 돌아왔을 때만 캐시를 살린다(`staleTime: Infinity` 포함). 🔴 **첫 구현은 언마운트 시 `window.location.pathname`을 읽었는데, `MemoryRouter`에서는 라우터 경로가 아니라 검증 불가능한 코드였다** — 게시판 B1에서 겪은 문제라 같은 방식으로 바꿨고 상세 왕복 시 요청이 1회로 유지되는 것을 테스트로 고정했다. 선택한 달 보존은 `history.replaceState` → 라우터 state로 옮겼다. `ChipBase.color`를 옵셔널로 완화했다(표에 없는 차량 유형이 오면 레거시가 색 없는 칩을 그린다). 레거시 기벽 재현: 거부 차량번호가 state에만 있어 새로고침 시 빈 값 전송(D-251) · 0분을 목록은 `0분` 상세는 `-`(D-249) · 사진 없는 칸만 비율이 바뀜(D-250). 테스트 351개 통과, parking 청크 51.4 kB / gzip 12.9 kB                                                                            |
| 2026-07-31 | **Parking PR3 이관 — 🎯 Parking 도메인 완료** (PK11 목록 · PK12·PK13 등록/재등록 · PK14 상세). **15개 화면 전부 이관됐다.** 달력을 `@vuepic/vue-datepicker` → **react-day-picker**로 교체하면서 **선택 규칙을 라이브러리에 맡기지 않았다** — `mode="range"`는 범위를 칠하는 용도로만 쓰고 값은 `onDayClick`이 계산한다(같은 날 무시 · 역순 교환 · 3번째 클릭 초기화). 여섯 시나리오를 테스트로 고정. 🔴 **타임존 보정**(`getTime() - offset`)을 그대로 옮기고 23:30으로 시계를 고정한 테스트로 못박았다 — 빼면 자정 근처에서 날짜가 하루 밀린다. 🔴 **크래시 1건을 고쳤다**(PK-Q10) — 재등록이 방문목적을 이름으로 되찾는데 삭제된 목적이면 레거시가 터진다. ✅ **PK-Q11 괄호 오타 수정 적용**. 등록·삭제의 에러 문구 표가 다르다는 것도 함께 옮겼다. 테스트 382개 통과, parking 청크 149.3 kB / gzip 40.1 kB (D-260)                                                                                                                                    |
| 2026-07-31 | **Visit PR1 이관 — 허브·키오스크·로비폰** (V1 `/visit` · V2 키오스크 비밀번호 · V3 로비폰 세대호출). **네이티브 브릿지를 처음 실제로 쓰는 도메인**이다 — SIP 상태 요청/수신과 경비 호출을 배선했다(Phase 4에서 만든 `shared/lib/native`가 그대로 맞았다). ✅ **레거시의 리스너 누수를 고쳤다**(D-262) — `emitter.on`만 있고 `off`가 없어 V3에 들어갈 때마다 익명 리스너가 쌓였다. `useEffect` cleanup으로 해소되면서 누수를 가리려고 넣어둔 **경로 검사도 뺐다**(V-Q3 결론). ✅ 비밀번호 4칸의 **문자열 `v-for` + 값 `key`**를 인덱스 키로 옮겨 `1123` 같은 중복 숫자에서 키가 충돌하지 않게 했다(V-Q4). 기록한 기벽: 구독 없으면 빈 화면(D-261) · 실패해도 모달이 닫힘(D-263) · 같은 모달인데 에러 처리가 화면마다 다름(D-264, V-Q5) · 백스페이스·붙여넣기 미지원(D-265) · 스피너가 모달을 덮음(D-266). 테스트 396개 통과, visit 청크 12.8 kB / gzip 4.0 kB                                                                                             |
