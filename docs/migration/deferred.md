# 이관 후로 미룬 개선 항목

> **등가 이관 원칙**: 이관 중에는 UI·동작을 바꾸지 않는다.
> 이관하면서 발견한 개선 아이디어는 여기에 적어두고, 전환 완료 후 별도 작업으로 처리한다.
>
> 여기 적힌 것을 이관 중에 고치면 "동작이 달라졌는지 내가 바꿔서인지"를 구분할 수 없게 된다.

## 사용법

- 발견 즉시 기록. 고치지 않는다
- 근거 파일·라인을 남긴다 (레거시 기준 SHA `6d5bf22`)
- 전환 완료(Phase 7) 후 우선순위를 매겨 처리한다

---

## 죽은 코드

| #     | 항목                                                                              | 근거                                                                                                                                                                                   | 비고                                                                                                                     |
| ----- | --------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| D-1   | `useDeleteAptMallMyOrder`의 폴백이 항상 `undefined`                               | `lib/queries/aptMall/useDeleteAptMallMyOrder.js:20` — `getParams()?.mealUuid`를 읽지만 실제 라우트 파라미터는 `aptMallOrderUuid`                                                       | 호출부가 값을 명시적으로 넘겨 동작에는 영향 없음. 이관 시 폴백 제거 가능                                                 |
| D-2   | `getNotOutCarHistorySummary`가 `getNotOutCarList`와 완전히 동일                   | `api/parking.js:307-330`                                                                                                                                                               | 경로·파라미터·구현 전부 동일. R-2 결정에 종속                                                                            |
| D-3   | `stores/resident.js` 전체 주석 처리                                               | `src/stores/resident.js`                                                                                                                                                               | 이관 대상 아님 (계획서 3-3에서 삭제로 확정)                                                                              |
| D-18  | 브릿지 `GO_APP_PERMISSION`·`CLEAR_APP_CACHE` 호출부 없음                          | `natives/apass.js:32`, `natives/common.js:25`                                                                                                                                          | **래퍼는 이관한다.** 프로토콜 상수라 앱이 기대할 수 있고 비용이 0에 가깝다. N-Q2 확인 후 정리                            |
| D-19  | `CALLBACK_PUSH_ALARM`의 emitter 폴백이 죽은 코드                                  | `natives/common.js:101`                                                                                                                                                                | 알려진 타입 2종은 항상 `router.push` 후 `return`하고, `emitter.on(CALLBACK_PUSH_ALARM)` 구독부가 없다                    |
| D-52  | `useGetAptMallMyOrderList`의 `setAdditionalParams`(`orderStatus` 필터) 호출부 0곳 | `lib/queries/aptMall/useGetAptMallMyOrderList.js:25-29`                                                                                                                                | 상태 필터 UI가 없다. **이관하지 않는다**                                                                                 |
| D-53  | `AptMallMyOrderView`가 넘기는 `onReservationClick` prop을 자식이 선언하지 않음    | `AptMallView/AptMallMyOrderView.vue:37` vs `AptMallMyOrderList.vue:11-27`                                                                                                              | 빈 상태에서 예약 유도 버튼을 붙이려다 만 흔적. 죽은 prop                                                                 |
| D-54  | `AptMallFormOrderConfirm`의 `textareaRef` 미사용                                  | `AptMallView/AptMallFormOrderConfirm.vue:24`                                                                                                                                           | 선언만 됨                                                                                                                |
| D-55  | `/aptMall/list`(AM1) 진입 경로 없음                                               | `router/AptMallIndex.js:4` — 메인 메뉴는 `/aptMall/myOrder` 직행, 딥링크·푸시 매핑에도 없음                                                                                            | `apt-mall.md` AM-Q2. 라우트째로 제외 후보                                                                                |
| D-67  | `useGetFireInspectionStatus`의 `refetchInspectionStatus` 호출부 0곳               | `lib/queries/fireInspection/useGetFireInspectionStatus.js:12,25`                                                                                                                       | 죽은 반환값                                                                                                              |
| D-68  | `useGetFireInspectionDetail`의 집계 3개 미사용                                    | `lib/queries/fireInspection/useGetFireInspectionDetail.js:31-37` — `normalCount`·`defectiveCount`·`notApplicableCount`                                                                 | 요약 UI를 만들려다 만 흔적. F4가 하나도 쓰지 않는다                                                                      |
| D-69  | `FireInspectionSignature`의 `dong`/`ho` prop 미배선                               | `FireInspectionView/Process/FireInspectionSignature.vue:4-10` vs `FireInspectionProcessView.vue:75`                                                                                    | 서명 문서에 세대 식별이 없다. `fire-inspection.md` F-Q12                                                                 |
| D-70  | `categoryNumber`가 `categoryId`와 항상 동일                                       | `constants/domain/fireInspection.js` — 10개 카테고리 전부                                                                                                                              | 중복 필드                                                                                                                |
| D-71  | `getItemImages`의 폴백 이미지 경로 도달 불가                                      | `useFireInspectionForm.js:194-200` — 21개 itemId가 모두 매핑돼 있다                                                                                                                    | 죽은 분기                                                                                                                |
| D-88  | `MOVING_HOUSE_TOAST_MESSAGE.copy`(`복사 되었습니다`)가 죽은 상수                  | `constants/domain/movingHouse.js:6` — 계좌 복사는 `COPIED` 모달(`복사가 완료되었습니다`)을 띄운다                                                                                      | 같은 뜻 문구가 두 벌, 한쪽만 쓰인다                                                                                      |
| D-89  | `MovingHouseView`가 `TabCategory`에 넘기는 `color="deepBlue"`                     | `MovingHouseView.vue:34` vs `components/common/TabCategory.vue:4-15` — `color` prop이 없다                                                                                             | `<ul>` 속성으로 떨어진다. 렌더 영향 없음                                                                                 |
| D-90  | `onSubmit`의 `moveDate: submitValues.moveDate` 중복                               | `MovingHouseWriteView.vue:145` — 바로 위 스프레드에 이미 포함                                                                                                                          | 무의미한 재지정                                                                                                          |
| D-110 | `RepairFormContainer`의 `defaultInfo` · `repairUuid` ref 미사용                   | `RepairView/RepairFormContainer.vue:30-31` — `defaultInfo`는 할당조차 없고 `repairUuid`는 `onMounted`에서만 설정                                                                       | 죽은 ref 2개                                                                                                             |
| D-111 | `RepairDetailContent`의 `desiredVisitDate` 분기                                   | `RepairView/RepairDetailContent.vue:32` — `REPAIR_DETAIL_CONTENT_FIELD`에 없는 키                                                                                                      | 죽은 분기                                                                                                                |
| D-112 | `useGetRepairStatusCount`의 `isRepairStatusCountLoading` 미사용                   | `lib/queries/repair/useGetRepairStatusCount.js:26` vs `RepairView.vue`                                                                                                                 | 로딩 중엔 칩만 보이고 숫자가 없다                                                                                        |
| D-113 | `RepairView`도 `TabCategory`에 `color="deepBlue"`를 넘긴다                        | `RepairView/RepairView.vue:88`                                                                                                                                                         | `color` prop 없음. D-89와 동일                                                                                           |
| D-114 | Repair 입력 필드의 `disabled:bg-*`가 비활성 안 되는 필드에도 붙어 있다            | `RepairView/RepairFormDetail.vue:106,124,141` 등                                                                                                                                       | 죽은 클래스                                                                                                              |
| D-134 | 🔴 `/managementFee/info`(524줄)가 도달 불가 + 전부 목업                           | `router/ManagementFeeIndex.js:4` — 라우터 정의 외 참조 0곳. `ManagementFeeInfoView.vue`에 `TODO: API 연동 시 실제 데이터로 교체` 주석 3곳, 금액·에너지·`12,892원`·`-21%` 전부 하드코딩 | **이관 제외 권장.** 제외하면 ApexCharts 대조가 `MainCardParkingMileageChart` 1건으로 줄어든다. `management-fee.md` MF-Q1 |
| D-135 | `useGetManagementFeeBill`의 `managementFeeBillRefetch` 미사용                     | `lib/queries/managementFee/useGetManagementFeeBill.js:31,53`                                                                                                                           | 에러 화면에 재시도 버튼이 없다                                                                                           |
| D-136 | `DrawerMonth`의 `initialYear`/`initialMonth` prop 미사용                          | `components/common/DrawerMonth.vue:8-15,29-30` — 호출부 2곳 모두 안 넘긴다                                                                                                             | 죽은 prop                                                                                                                |
| D-137 | `DrawerMonth`의 `is-close="true"`가 무효                                          | `components/common/DrawerMonth.vue` — `title`을 넘기지 않아 `DrawerBase`의 헤더(`v-if="title"`)가 렌더되지 않는다                                                                      | **X 버튼이 없고 배경 클릭만 가능.** `management-fee.md` MF-Q9                                                            |
| D-138 | `DrawerMonth`가 정렬을 두 번 다르게 구현한다                                      | `components/common/DrawerMonth.vue` — 목록은 숫자 비교 `sort`, 최신값 선택은 문자열 `sort().reverse()`                                                                                 | 같은 일을 두 방식으로                                                                                                    |
| D-139 | 관리비 상세의 불필요한 래퍼 div                                                   | `ManagementFeeView/ManagementFeeDetailView.vue` — 상세내역 항목의 `flex flex-col gap-0.5` 안에 `<span>` 하나뿐                                                                         | 두 번째 줄을 넣으려다 만 흔적                                                                                            |
| D-150 | A-PASS `currentX` ref 미사용                                                      | `ApassView/ApassActivityButton.vue:20,73-78` — 170/0으로 설정하지만 템플릿에서 쓰이지 않는다                                                                                           | 슬라이드 토글을 만들려다 탭 방식으로 바꾼 흔적                                                                           |
| D-151 | A-PASS 토글의 빈 `<div>`                                                          | `ApassView/ApassActivityButton.vue:88` — `absolute bottom-[18px] left-1/2 translate-x-[-50%]`, 자식·내용 없음                                                                          | 죽은 마크업                                                                                                              |
| D-152 | `patchAPassActiveMutation(isActive)`의 인자가 무시된다                            | `ApassView/ApassActivityButton.vue:52` vs `usePatchAPassActive.js:16-18` — API가 본문 없는 토글                                                                                        | 호출 전 가드에서만 값을 쓴다                                                                                             |
| D-153 | `ApassOn.svg`·`ApassOff.svg`가 미사용 자산                                        | `public/assets/icons/aPass/` — 코드는 `.png`를 쓴다                                                                                                                                    | SVG 교체 시도 흔적                                                                                                       |
| D-168 | `globalColor.scss` 주입이 죽은 설정                                               | `vite.config.js:39-43` — 8개 `lang="scss"` 블록 중 `$` 변수를 쓰는 곳이 **0곳**                                                                                                        | **이식하지 않는다.** 단 이 파일이 `broken-styles.md` §5의 답을 갖고 있었다 (`$deep-blue-20`·`$bg-gray`)                  |
| D-169 | `lang="scss"` 8개 중 SCSS 문법을 쓰는 파일이 1개                                  | `BoardView/NoticeBoard/NoticeDetailView.vue`의 `//` 주석. 나머지 7개는 `calc()`·`!important`·CSS 변수뿐                                                                                | 타깃에서는 전부 일반 CSS로                                                                                               |
| D-170 | opinion 앱 번들에 메인 앱 코드가 함께 들어갈 수 있다                              | `main.js:11-14` — `MainApp`·`OpinionApp`·양쪽 라우터를 조건 밖에서 정적 import                                                                                                         | 타깃은 엔트리 물리 분리로 해결. `opinion.md` O-Q1                                                                        |
| D-171 | opinion 앱이 네이티브 브릿지 4모듈을 side-effect import한다                       | `main.js:17-20` — apass·auth·face·lobbyPhone                                                                                                                                           | 브라우저에서 열리는 앱인데 `window.CALLBACK_*`가 등록된다. `CALLBACK_GO_BACK`만 필요                                     |
| D-172 | opinion 앱이 `VCalendar`·`VueDatePicker`를 등록한다                               | `main.js:58,60` — 달력을 쓰지 않는다                                                                                                                                                   | `VCalendar`는 전체 제거 확정 (0-5)                                                                                       |
| D-173 | eruda가 `MODE === 'development'` 정확 비교라 opinion dev에 안 붙는다              | `main.js:27`                                                                                                                                                                           | 의도 불명. 타깃은 eruda를 쓰지 않는다                                                                                    |
| D-174 | `LayoutOpinionBase`의 `h-[calc(100%-67px)]` 바텀네비 분기가 죽었다                | `components/layouts/LayoutOpinionBase.vue` — opinion 라우트에 `showBottomNav: true`가 없다                                                                                             |                                                                                                                          |
| D-175 | opinion 앱이 `version.json`을 배포하지만 읽지 않는다                              | `aws-deploy.yml`이 두 빌드 모두 업로드. `reloadIfNewVersion`은 `router/index.js`에만 있다                                                                                              | `opinion.md` O-Q8                                                                                                        |
| D-154 | A-PASS API 2개만 인자를 객체로 받지 않는다                                        | `api/apass.js:5,13` — `getIsAPassActive(residentUuid)`                                                                                                                                 | 148개 중 이 2개뿐. 호출부 2곳이라 통일 가능                                                                              |
| D-155 | `useGetIsAPassActive`의 `enabled`가 비반응형                                      | `lib/queries/apass/useGetIsAPassActive.js:16` — `!!authStore.accessToken` (ref/computed 아님)                                                                                          | 토큰이 늦게 오면 영구 비활성. 타깃에서는 자동으로 반응형                                                                 |

## 구조 개선

| #     | 항목                                                   | 근거                                                                                                                       | 비고                                                                                                                                       |
| ----- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| D-4   | 관리비 상세의 연월 선택을 URL 쿼리로                   | `ManagementFeeView/ManagementFeeDetailView.vue:12-13` — 로컬 `ref`                                                         | 타깃 `04-state.md`의 "공유 가능한 상태는 URL" 규칙에 맞음. 다만 새로고침·뒤로가기 동작이 달라지므로 이관 중에는 금지                       |
| D-5   | 민원공간 리스트 경로만 `/complaint/list`               | `api/board.js:400-414` (소통공간은 `/community`)                                                                           | 서버 계약이라 프론트 단독 변경 불가. 백엔드 정리 시 함께                                                                                   |
| D-6   | 게시판 댓글 라우트만 `/post/...` 접두사                | `router/BoardIndex.js` #32,33,39,40                                                                                        | 나머지는 `/board/...`. 경로 일관성                                                                                                         |
| D-7   | FireInspection만 kebab-case 경로                       | `router/FireInspectionIndex.js` — `/fire-inspection`                                                                       | 나머지 도메인은 camelCase                                                                                                                  |
| D-8   | `apass` 라우트의 무의미한 meta                         | `router/ApassIndex.js` — `showAppBar:false`인데 `appBarTitle`·`hasBackButton` 지정                                         | 무시되는 값                                                                                                                                |
| D-84  | 소방점검이 composable 반환 객체를 props로 2단 내린다   | `Process/FireInspectionProcessView.vue:87` → `FireInspectionCategory.vue:127` → `FireInspectionItem.vue:14`                | props drilling. 타깃에서는 Context로. `fire-inspection.md` §3                                                                              |
| D-85  | Vote `CanvasSign`과 소방 서명 캔버스가 중복 구현       | `components/common/CanvasSign.vue` vs `Process/FireInspectionSignature.vue`                                                | 공용 `SignaturePad` 코어 + 화면별 껍데기. `fire-inspection.md` F-Q13                                                                       |
| D-86  | 소방점검 F2a와 F4의 타이포·패딩이 다르다               | 헤더 `px-3 py-5`/`16SemiBold` vs `px-5 py-4`/`18Medium`, 항목 라벨 `18SemiBold` vs `16SemiBold`                            | 같은 점검표인데 글자 크기가 다르다. 등가 이관으로 각각 유지                                                                                |
| D-87  | 소방점검 `lodash` 8개 함수를 표준 JS로 대체 가능       | `useFireInspectionForm.js:1`, `FireInspectionHistoryDetailView.vue:2` — `every filter forEach get isEmpty size sumBy find` | **`lodash-es` 추가 없이 이관할 수 있다.** `tech-mapping.md` 3-5                                                                            |
| D-106 | 이사예약 상세/확인 컴포넌트가 경로 문자열로 분기한다   | `MovingHouseDetailBasicContent.vue:35-37` — `getCurrentRoutePath().includes('detail')`                                     | `mode` prop이어야 한다. `moving-house.md` MH2                                                                                              |
| D-107 | 이사예약이 board 도메인 상수를 import한다              | `MovingHouseWriteView.vue:13` — `WRITE_BACK_MODAL_DATA` from `constants/domain/board.js`                                   | feature 간 직접 참조. `shared/constants/`로 올린다                                                                                         |
| D-108 | `TabCategory`의 선택 상태가 컴포넌트 내부 ref다        | `components/common/TabCategory.vue:19` — URL·부모와 동기화 없음                                                            | 상세를 다녀오면 `전체`로 초기화된다 (스크롤은 복원)                                                                                        |
| D-109 | 이사예약 훅별로 `getAptInfo()` 옵셔널 체이닝이 다르다  | `List`·`Setting`·`TimeList`·`HolidayList`는 없고 `Detail`·`Delete`·`Post`는 있다                                           | 통일                                                                                                                                       |
| D-131 | `validImage`가 Pinia 스토어 객체를 직접 받는다         | `lib/utils/validImage.js` — `store.imageList` · `store.setFieldValue`                                                      | Board·Repair 공용 유틸인데 스토어 형태에 결합. 순수 함수로 분리                                                                            |
| D-132 | Repair 업로드 진행률이 뷰 → 스토어 → 뷰로 왕복한다     | `RepairWriteView.vue:12-14` → `stores/repair.js` → `RepairFormContainer.vue:111`                                           | 같은 트리 안인데 스토어를 경유. mutation 훅을 직접 구독하면 된다                                                                           |
| D-133 | Repair가 board 도메인 상수 2개를 import한다            | `RepairView/RepairFormContainer.vue:10-13,122` — `WRITE_BACK_MODAL_DATA`·`EDIT_BACK_MODAL_DATA`                            | D-107(이사예약)과 동일. `shared/constants/`로                                                                                              |
| D-167 | `CALLBACK_PERMISSION_INFO` 리스너가 두 곳에서 누수된다 | `ApassView/ApassView.vue:54-58` · `MainView/MainCardMenu/MainCardApassBadge.vue:26-30` — 익명 핸들러 + `off` 없음          | **경로 문자열 가드가 누수를 전제로 한 설계다.** `visit.md`의 SIP 리스너 누수와 같은 패턴. 타깃에서는 `usePermissionInfo` 공용 훅 + cleanup |

## 서버 계약 정리 (백엔드 협의 필요)

| #    | 항목                                | 근거                                                                    | 비고                                                                    |
| ---- | ----------------------------------- | ----------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| D-9  | 관리비 조회 쿼리 파라미터 오타      | `api/managementFee.js:20-21` — `startDateTIme`/`endDateTIme` (대문자 I) | **이관 중 절대 수정 금지.** 고치면 조회가 깨진다. 서버와 동시 변경 필요 |
| D-10 | 정렬 파라미터 이름 불일치           | `desc`(`api/parking.js:288`) vs `isDesc`(`api/parking.js:360`)          |                                                                         |
| D-11 | 투표/설문 상태 파라미터 이름 불일치 | `voteStatus`(`api/vote.js:7`) vs `state`(`api/survey.js:7`)             | 같은 개념                                                               |
| D-12 | 안면인식 식별자만 `Guid`            | `api/faceRegister.js` — 나머지는 전부 `Uuid`                            | 외부 시스템 식별자로 추정                                               |

## 오타·표기

| #     | 항목                                                                        | 근거                                                                                                                        |
| ----- | --------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| D-13  | opinion 라우트명에 공백 2칸                                                 | `router/SurveyExternalIndex.js:65` — `설문  이름 휴대전화 인증`                                                             |
| D-14  | `LoginIndex`의 별칭 불일치                                                  | `router/LoginIndex.js:5` — `@/views/...` (나머지는 `@views/...`)                                                            |
| D-20  | 버전1 약관 화면의 죽은 `<style scoped>`                                     | `LoginView/VersionOneTermsView.vue:22-26` — `.fixed-width`가 템플릿에서 안 쓰임. **이관하지 않는다**                        |
| D-63  | 아파트몰 스토어 id가 `mealForm`                                             | `stores/aptMall.js:4` — `defineStore('mealForm', ...)`. `useDeleteAptMallMyOrder.js`도 내부 함수명이 `useDeleteMealMyOrder` | 도메인명이 `meal`→`aptMall`로 바뀐 잔재. **내부 식별자라 이관 시 정리**                                         |
| D-64  | `AptMallMyOrderListItem`의 `// 투표상태` 주석                               | `AptMallView/AptMallMyOrderListItem.vue:21`                                                                                 | Vote에서 복붙한 잔재                                                                                            |
| D-65  | AM1 라우트 `appBarBackgroundColor: 'rgba(248,248,248)'`                     | `router/AptMallIndex.js:12` — `rgba()`에 인자 3개                                                                           | CSS Color 4에서 `rgb()` 별칭으로 허용돼 `#f8f8f8`로 렌더. AM2는 같은 자리에 `#f3f4f6`. `apt-mall.md` AM-Q1      |
| D-66  | 아파트몰 AM11 `<ol>`이 가로 배치인데 `overflow-y-scroll`                    | `AptMallView/AptMallFormOrderCalendarTime.vue:82`                                                                           | 축 불일치. `apt-mall.md` AM-Q21                                                                                 |
| D-79  | 소방점검 이미지 경로에 한글이 들어 있다                                     | `constants/domain/fireInspection.js:260-307` — `/assets/images/자가점검표/소화기/...`                                       | 현재 배포 환경에서는 동작. 경로 인코딩에 민감한 환경으로 바뀌면 깨진다. `fire-inspection.md` F-Q3               |
| D-80  | 소방점검 시작 모달 아이콘이 `Link.svg`(링크 아이콘)                         | `components/FireInspectionStartModal.vue:36` — `brightness-0 invert`로 흰색 반전                                            | 소방/점검과 무관한 아이콘. `fire-inspection.md` F-Q9                                                            |
| D-81  | `InputRadioList` disabled 배경이 하드코딩 hex                               | `components/common/InputRadioList.vue` — `bg-[#e7e7e7] opacity-50`                                                          | 토큰 아님                                                                                                       |
| D-82  | 소방 서명 캔버스 `strokeStyle`이 하드코딩 hex                               | `Process/FireInspectionSignature.vue:46` — `#1F2937`                                                                        | `neutral-b-gray-800` 계열                                                                                       |
| D-83  | `FireInspectionStatusCard`만 `useRouter`를 직접 쓴다                        | `components/FireInspectionStatusCard.vue:16`                                                                                | 도메인 내 유일 — `useNavigate`로 통일                                                                           |
| D-101 | 이사예약 모달 문구 `관리자 사무소` (`관리사무소` 오타)                      | `constants/domain/movingHouse.js:62` — `CONFIRMED.description[2]`                                                           | 화면에 보이는 오탈자. `moving-house.md` MH-Q2                                                                   |
| D-102 | 이사예약 입금자명 에러가 `이름을 입력해주세요`                              | `schemas/movingHouse.js:30` — 공용 `name` 스키마 재사용                                                                     | 라벨은 `입금자명`. `moving-house.md` MH-Q1                                                                      |
| D-103 | `MOVING_HOUSE_LIST_ITEM_FIELD`의 `label: '유형 '` 끝 공백                   | `constants/domain/movingHouse.js:23`                                                                                        | HTML 공백 축약으로 무해                                                                                         |
| D-104 | `WAITING` 모달 문구의 공백 2칸                                              | `constants/domain/movingHouse.js:51` — `취소하시면  접수`                                                                   | 〃                                                                                                              |
| D-105 | 달력 5개 인스턴스의 `week-start`·`no-today`가 서로 다르다                   | AptMall·주차는 `week-start="0"`(일), 이사예약·로비폰 2개는 미지정(→`1` 월). `no-today`는 2:3                                | `@vuepic/vue-datepicker@9.0.3` 기본값 `weekStart: 1` 확인. 공용 래퍼 기본값 결정 필요. `moving-house.md` MH-Q12 |
| D-126 | `처리 불가된 접수는 수정할 수 없습니다` 문법이 어색하다                     | `constants/domain/repair.js:20,53-58` — 상태 라벨을 문구에 재사용                                                           | `repair.md` RP-Q6                                                                                               |
| D-127 | `파일 사이즈는 10M 이하만 업로드 가능 합니다` 띄어쓰기                      | `constants/domain/repair.js:11`                                                                                             | 실제 기준은 `10000000`B ≈ 9.54MiB                                                                               |
| D-128 | `repairStatusCount` 조회에 `toLocaleLowerCase()`를 쓴다                     | `RepairView/RepairView.vue:64`                                                                                              | ASCII만 다루므로 결과 동일. 터키어 로케일에서 `I`→`ı`                                                           |
| D-129 | `<div for="imageUpload">` — 라벨이 아닌 요소의 `for`                        | `RepairView/RepairFormImage.vue`                                                                                            | 무의미한 속성                                                                                                   |
| D-130 | Repair RP3·RP4 meta의 `hasBackButton: false`가 무효                         | `router/RepairIndex.js:31,40` — `showAppBar: false`라 레이아웃 AppBar가 없다                                                | D-8(apass)과 같은 계열                                                                                          |
| D-146 | 관리비 아이콘 파일명 비대칭                                                 | `public/assets/icons/icon-minus-circle-common.svg`(단수) vs `icons-plus-circle-common.svg`(복수)                            | 실제 파일명. 파일을 그대로 복사하므로 이관에 영향 없음                                                          |
| D-147 | 관리비 MF2 라우트 name(`관리비 정보`)과 AppBar 제목(`관리비 조회`)이 다르다 | `router/ManagementFeeIndex.js:5,11`                                                                                         | D-134 결정에 종속                                                                                               |
| D-148 | `SkeletonBase` 기본색이 하드코딩 hex 8자리                                  | `components/common/SkeletonBase.vue:6` — `bg-[#CDCBCBFF]`                                                                   | shadcn `skeleton`은 `bg-muted`. 레거시 색으로 덮어야 한다                                                       |
| D-149 | 관리비 아코디언이 `<button>`이 아니라 `<div @click>`이다                    | `ManagementFeeDetailView.vue` 2곳 · `DrawerMonth.vue` 트리거                                                                | 키보드 접근 불가                                                                                                |
| D-162 | A-PASS 토글 이미지 `alt="화살표 아이콘"`                                    | `ApassView/ApassActivityButton.vue:99` — 화살표가 아니다                                                                    | 복붙 잔재                                                                                                       |
| D-163 | A-PASS 체크 아이콘 `alt`이 on/off 동일                                      | `ApassView/ApassPermissionItem.vue:53-58` — 둘 다 `선택 아이콘`                                                             | 상태를 구분하지 않는다                                                                                          |
| D-164 | `위치 항상허용` 뒤 공백 2칸                                                 | `ApassView/ApassView.vue:37` — 템플릿 리터럴                                                                                | HTML 공백 축약으로 무해                                                                                         |
| D-165 | A-PASS 헤더 그라데이션이 하드코딩 hex                                       | `ApassView/ApassView.vue:66` — `bg-[linear-gradient(293deg,#2f85df_4.47%,#429eff_119.83%)]` · `text-white`(토큰 아님)       | 소수점 픽셀 크기 4개도 함께                                                                                     |
| D-180 | `AppBar`가 `import.meta.env.MODE`를 직접 읽는다                             | `components/layouts/AppBar.vue:31` — `MODE.includes('opinion')`으로 `max-w-[480px]` 분기                                    | 타깃 절대 규칙 1 위반. **prop/Context로 내리는 것을 권장.** `opinion.md` O-Q9                                   |
| D-181 | `manualChunks`가 `@scope/name`을 `@scope`로 자른다                          | `vite.config.js:57` — `split('/')[0]`                                                                                       | `@tanstack/vue-query`와 devtools가 같은 청크. 빌드 산출물 구조라 사용자에게 안 보인다                           |
| D-182 | Sentry `org`·`project`가 하드코딩                                           | `vite.config.js:19-20` — `smcom` / `apt-resident-fe`                                                                        | Phase 7 레포 전환 시 project 이름 결정. `opinion.md` O-Q3                                                       |
| D-183 | `LayoutOpinionBase`의 `w-screen`(100vw)                                     | 데스크톱 스크롤바에서 가로 오버플로                                                                                         | `max-w-[480px]`가 실제 폭을 정하므로 눈에 띄지 않는다                                                           |
| D-166 | 메인 A-PASS 배지 `사용중`만 `ChipBase`를 쓰지 않는다                        | `MainCardApassBadge.vue:53-73` — 초록 점 인라인 SVG 때문. `rounded-[31px]`·`bg-[rgba(0,187,64,0.1)]`·`#00BB40` 하드코딩     | `ChipBase` `deepGreen`과 같은 색 계열                                                                           |

## 동작 의심 (등가 이관으로 그대로 옮기되 확인 필요)

| #     | 항목                                                                          | 근거                                                                                                                                                                                           | 비고                                                                                                                                                                                        |
| ----- | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-21  | 비밀번호 인증 "재요청" 버튼이 1회만 동작                                      | `LoginView/PasswordPhoneCertView.vue:128-136` — `resendCodeValue`가 `false`가 된 뒤 복원되지 않음                                                                                              | `features/auth.md` A-Q1. 의도인지 확인                                                                                                                                                      |
| D-22  | 인증 성공 시 화면이 잠깐 빈 상태가 됨                                         | `LoginView/PasswordPhoneCertView.vue:159` — 루트 `v-if="!isSuccess"`                                                                                                                           | `features/auth.md` A-Q2. 깜빡임 실기기 확인                                                                                                                                                 |
| D-23  | opinion 에러 화면에 `ERROR : undefined` 노출 가능                             | `ExceptionView/OpinionExternalErrorView.vue:30` — `ref(undefined)` + 조건 렌더 없음. 메인 앱 E1은 `v-if`로 가림                                                                                | `features/exception.md` X-Q1                                                                                                                                                                |
| D-24  | `loginDataHandler` 실패를 조용히 삼킴                                         | `lib/composables/useLoginData.js:47-49` — `console.error`만. 부트스트랩 실패인데 로그인은 성공 처리                                                                                            | `features/auth.md` A-Q3                                                                                                                                                                     |
| D-25  | `setSignUpInfo({})`가 초기화되지 않음 (병합만 함)                             | `stores/auth.js:6-12` — `{...prev, ...{}}`는 no-op. S1·A2 마운트의 "초기화" 의도가 동작하지 않는다                                                                                             | `features/signup.md` S-Q2. 이전 가입 시도 데이터가 남는다                                                                                                                                   |
| D-26  | `selectedAptUuid` 초기값이 `'aaaa'` 하드코딩                                  | `SignUpView/SignUpAptInfoView.vue:54`                                                                                                                                                          | `aptName` 필수 검증이 막고 있어 실제 전송은 안 되지만 명백한 지뢰                                                                                                                           |
| D-27  | 회원가입 완료로 넘기는 `state.pageFrom`을 아무도 읽지 않음                    | `usePostUserInfo.js:59` → `SignUpCompletedView.vue`                                                                                                                                            | 죽은 state                                                                                                                                                                                  |
| D-28  | 알 수 없는 `termsId`면 빈 화면                                                | `TermsOfUseView/TermsOfUseDetailView.vue:19` — `v-if="termsItem"`, 폴백 없음                                                                                                                   | 에러 처리 없음                                                                                                                                                                              |
| D-29  | 아파트 검색 모달이 열릴 때 빈 키워드로 API 1회 호출                           | `lib/queries/auth/useGetAptList.js:10-14` — `enabled` 가드 없음                                                                                                                                | 불필요한 요청. `features/signup.md` S-Q6                                                                                                                                                    |
| D-30  | `useGetAptList`의 `watch` 무효화가 불필요                                     | `lib/queries/auth/useGetAptList.js:20-22` — 키에 `keyword`가 이미 있어 자동 refetch됨                                                                                                          | **이관 시 옮기지 않아도 동작 동일**                                                                                                                                                         |
| D-33  | `pushAuthorized`를 저장만 하고 안 씀                                          | `MainView/MainView.vue:28,37`                                                                                                                                                                  | `features/main.md` M-Q5                                                                                                                                                                     |
| D-37  | 방문예약 카드의 `shouldShake`가 템플릿에서 미사용                             | `MainCardMenu/MainCardReservation.vue:15,23-28` — 애니메이션은 CSS 클래스로 항상 실행                                                                                                          | 300ms 지연 로직이 무의미                                                                                                                                                                    |
| D-38  | `FIXED_MENUS`의 `'공지사항'`이 죽은 항목                                      | `useGetResidentDetailInfo.js:25` — `MAIN_SWIPER_MENU_LIST`에 해당 `menuName`이 없어 필터에 안 잡힘                                                                                             | 실제 고정 메뉴는 `관리사무소` 하나뿐                                                                                                                                                        |
| D-41  | 콘텐츠 플래그 4개가 미사용                                                    | `useGetResidentDetailInfo.js` — `hasAptCommunityContent`·`hasAptMovingHouseContent`·`hasAptVoteContent`·`hasAptShoppingContent` 사용처 0곳                                                     | 해당 메뉴는 `MAIN_SWIPER_MENU_LIST.contentName`으로 게이팅. **이관 시 만들지 않아도 동작 동일**                                                                                             |
| D-42  | 에테르노 단지 UUID가 상수와 리터럴로 중복 정의                                | `MainAdvertisementBanner.vue:23` vs `:45-46`                                                                                                                                                   | 같은 값을 두 번 씀                                                                                                                                                                          |
| D-43  | `MyPageMenuList.filterGroupList`가 `null`을 걸러내지 않음                     | `MyPageView/MyPageMenuList.vue:79-92` — `.map()`이 `null` 반환 가능한데 `.filter(Boolean)` 없음                                                                                                | 게시판 그룹 첫 항목이 무조건 통과해 실제로는 발생 안 함                                                                                                                                     |
| D-44  | 앱 버전이 항상 `버전 없음`으로 표시                                           | `MyPageView/MyPageVersion.vue:10` — `localStorage['version']`을 쓰는 곳이 코드 전체에 없고 `getApartmantVersion` API도 호출부가 없다                                                           | `features/mypage.md` P-Q2. 네이티브가 직접 쓰지 않는 한 죽은 기능                                                                                                                           |
| D-45  | `MyProfileView.userInfos`가 `computed`가 아님                                 | `MyProfile/MyProfileView.vue:11` — 마운트 시점 값으로 고정                                                                                                                                     | React에서는 렌더 중 계산이라 자동 반응형이 됨                                                                                                                                               |
| D-46  | 알림 설정의 `isDisabled`가 무시됨                                             | `useAlarmSetting.js:238`에 정의했으나 `AlarmSettingMenuGroupItem.vue:44`가 `isPending`만 전달                                                                                                  | 마케팅 미동의 상태에서도 광고성 토글이 활성화                                                                                                                                               |
| D-47  | `fontSizeLabel` 폴백에 TypeError                                              | `storage/useFontSizeStorage.js:17-20` — `LABELS.ETC_FONT_SIZE_SCALES.MEDIUM`은 `undefined.MEDIUM`                                                                                              | 정상 경로에선 실행 안 됨. **고쳐도 등가**                                                                                                                                                   |
| D-48  | `MyPageFontSizeItem.vue` 미사용                                               | import하는 곳 0곳. 슬라이더로 대체된 흔적                                                                                                                                                      | **이관하지 않는다**                                                                                                                                                                         |
| D-56  | 아파트몰 상세 총액이 수량을 곱하지 않는다                                     | `AptMallView/AptMallMyOrderDetailMenuList.vue:12-14` — `sum + item.price`. 확인 단계(`AptMallFormOrderConfirm.vue:110`)는 `item.count * item.price`                                            | `apt-mall.md` AM-Q10. 서버 `price`가 단가인지 줄 합계인지 확인 필요                                                                                                                         |
| D-57  | 아파트몰 잔여석 판정이 문자열 산술                                            | `AptMallView/AptMallFormOrderCalendarTime.vue:28-62` — `toLocaleString()` 결과에서 인원 수를 뺀다                                                                                              | 잔여석 1,000 이상이면 `NaN`. `TAKEOUT`은 `personCount`가 없어 판정 자체가 무효. `apt-mall.md` AM-Q17                                                                                        |
| D-58  | 아파트몰 예약 실패 시 두 번째부터 모달이 안 뜬다                              | `AptMallView/AptMallFormOrderConfirm.vue:72-77` — `watch(isPostAptMallOrderError)`가 `false→true` 전환만 감지                                                                                  | `mutateAsync` rejection도 잡히지 않는다. `apt-mall.md` AM-Q20                                                                                                                               |
| D-59  | 아파트몰 드로어를 닫지 않고 화면을 벗어나면 위저드 상태가 남는다              | `AptMallView/AptMallForm.vue:43-46` — `resetAptMallFormData`가 `close` 경로에만 있다                                                                                                           | 다음 예약에 이전 메뉴 수량이 보인다. `apt-mall.md` AM-Q6                                                                                                                                    |
| D-60  | 아파트몰 초기 선택일 폴백이 비운영일일 수 있다                                | `AptMallView/AptMallFormOrderCalendarDate.vue:36-53` — 못 찾으면 `today` 반환, `reservationLimitDays`가 0/없으면 루프 0회                                                                      | 시간대가 빈 화면이 된다. `apt-mall.md` AM-Q15                                                                                                                                               |
| D-61  | 아파트몰 인원 상한 10이 클라이언트 하드코딩                                   | `AptMallView/AptMallFormOrderCalendarPersonCount.vue:9`                                                                                                                                        | 잔여석보다 큰 인원 선택 가능. `apt-mall.md` AM-Q16                                                                                                                                          |
| D-62  | `['aptMallList']`·`['aptMallDetail']` 키에 `aptResidentUuid`가 없다           | `useGetAptMallList.js:14`, `useGetAptMallDetail.js:14`                                                                                                                                         | 단지 전환 시 캐시 오염. `apt-mall.md` AM-Q8                                                                                                                                                 |
| D-72  | 소방점검 3번 카테고리의 항목 설명이 **"주차장 소화장치"** 라고 잘못 적혀 있다 | `constants/domain/fireInspection.js:90-107`(+ 274줄 주석 `주거용 주차장 소화장치`) — 카테고리명은 `주거용 주방자동 소화장치`, `tooltipText`는 주방이 맞다                                      | 화면에 보이는 문구. `groupId: PARKING_EXTINGUISHER`는 서버 enum이라 유지. `fire-inspection.md` F-Q2                                                                                         |
| D-73  | 소방점검 `해당없음` 해제 시 그 카테고리의 응답이 전부 삭제된다                | `useFireInspectionForm.js:155-157` — `delete inspectionResults.value[item.itemId]`                                                                                                             | 정상/불량을 다 고른 뒤 실수로 켰다 끄면 입력이 사라진다. `fire-inspection.md` F-Q5                                                                                                          |
| D-74  | 소방점검 F1이 `inspectionList[0]`을 최신 회차로 가정                          | `FireInspectionView.vue:20` — 정렬 없음                                                                                                                                                        | 서버 정렬이 뒤바뀌면 과거 회차로 점검을 시작한다. `fire-inspection.md` F-Q7                                                                                                                 |
| D-75  | 소방점검 내역 0건이면 헤더 버튼이 `점검 기간이 종료되었습니다.`               | `FireInspectionHeader.vue:36-42` — `NOT_PARTICIPATED`와 `default`가 fall-through                                                                                                               | 점검이 한 번도 없던 단지에 부정확한 문구. `fire-inspection.md` F-Q8                                                                                                                         |
| D-76  | 소방점검 F4의 도움말 아이콘에 클릭 핸들러가 없다                              | `History/FireInspectionHistoryDetailView.vue:99-105` — F2a에서 복사하며 툴팁 배선 누락                                                                                                         | 눌러도 아무 일이 없다. `fire-inspection.md` F-Q16                                                                                                                                           |
| D-77  | 소방점검 F4 조회 실패 시 빈 흰 화면                                           | `History/FireInspectionHistoryDetailView.vue:72` — `v-else-if`에 `v-else`가 없다                                                                                                               | 에러 UI 없음. `fire-inspection.md` F-Q15                                                                                                                                                    |
| D-78  | 소방점검 `홈으로 돌아가기`가 인트로를 경유한다                                | `Complete/FireInspectionCompleteView.vue:9` — `navigateTo('/')` → `redirect: 인트로` → 가드가 `메인`으로                                                                                       | `getLoginInfo()` 실패 시 `clearAuth()` → **로그아웃**. Vote·Survey는 `/main` 직행. `fire-inspection.md` F-Q14                                                                               |
| D-91  | 이사예약 목록 필터 초기값이 빈 객체 `{}`                                      | `useGetMovingHouseList.js:10` — 탭을 누르면 `undefined`가 되지만 최초 요청은 `{}`                                                                                                              | 최초 쿼리스트링 실물 확인 필요. `moving-house.md` MH-Q6                                                                                                                                     |
| D-92  | 이사예약 시간대 훅이 `select` 안에서 외부 ref를 변경한다                      | `useGetMovingHouseReservationTimeList.js:47` — `timeSlotRadioList.value = ...`                                                                                                                 | `select`는 순수해야 한다. 파생 값으로. `moving-house.md` §4-3                                                                                                                               |
| D-93  | 이사예약 상세가 MH4용 시간대 쿼리를 기다려 스피너를 띄운다                    | `MovingHouseDetailBasicContent.vue:31-32,157` — 공유 컴포넌트라 상세에서도 호출                                                                                                                | 불필요한 API 1회 + 지연. `moving-house.md` MH-Q9                                                                                                                                            |
| D-94  | 이사예약 안내문이 `<p>` 안에 `<div>`를 담아 배경·패딩이 사라진다              | `MovingHouseDetailInfo.vue:14-24` — `chargeFlag: true`일 때만                                                                                                                                  | 파서가 `<p>`를 강제로 닫는다. `moving-house.md` MH-Q10                                                                                                                                      |
| D-95  | 이사예약 목록 카드의 `moveType` 조회에 `?.`가 없다                            | `MovingHouseListItem.vue:32-36` — 상세판(`MovingHouseDetailBasicContent.vue:91-95`)에는 있다                                                                                                   | 미지의 유형값이면 카드 크래시. `moving-house.md` MH-Q7                                                                                                                                      |
| D-96  | 이사예약 `다음` 버튼이 회색이지만 눌린다                                      | `MovingHouseWriteView.vue:349` — `:color`만 바꾸고 `:disabled`가 없다                                                                                                                          | AptMall·소방은 `disabled`. `moving-house.md` MH-Q13                                                                                                                                         |
| D-97  | 이사예약 MH4를 직접 URL/새로고침으로 열면 깨진다                              | `MovingHouseWriteConfirmView.vue:15,24,30` — Pinia가 비면 전 필드 `-`, 제출 시 TypeError, `navigateBack(-2)`가 앱 밖으로                                                                       | 가드 없음. `moving-house.md` MH-Q15                                                                                                                                                         |
| D-98  | 이사예약 `예약확정` 실패 시 unhandled rejection                               | `MovingHouseWriteConfirmView.vue:29-32` — `mutateAsync`를 잡지 않는다                                                                                                                          | AptMall AM-Q20과 같은 패턴. `moving-house.md` MH-Q14                                                                                                                                        |
| D-99  | 이사예약 안내문의 `14일`이 하드코딩                                           | `constants/domain/movingHouse.js:86-95`                                                                                                                                                        | 서버 설정값 여부 확인 필요. `moving-house.md` MH-Q3                                                                                                                                         |
| D-100 | 이사예약 상세 값이 전부 `v-dompurify-html`이다                                | `MovingHouseDetailBasicContent.vue:178` — 메모만 HTML이 필요한데 예약번호·유형·사용료까지                                                                                                      | `moving-house.md` MH-Q8                                                                                                                                                                     |
| D-115 | 🔴 하자보수 목록의 무한 스크롤 관측 타깃이 템플릿에 없다                      | `RepairView/RepairList.vue:27` — `target = ref(null)`을 선언하고 `useIntersectionObserver`를 붙였으나 `ref="target"` 요소가 없다. 다른 목록 7곳에는 모두 있다                                  | **2페이지가 로드되지 않는다.** 11건 이상이면 10건만 보인다. `repair.md` RP-Q4                                                                                                               |
| D-116 | 🔴 `isWritingPage`가 `'write'`를 찾는데 라우트는 `/repair/create`다           | `RepairView/RepairFormContainer.vue:34`                                                                                                                                                        | 등록 화면 제목이 `하자보수 수정`, 뒤로가기 모달이 `수정 그만두기`. `repair.md` RP-Q1                                                                                                        |
| D-117 | 🔴 하자보수 등록 화면에 AppBar가 2개 겹친다                                   | `router/RepairIndex.js:20`(meta `showAppBar: true`) + `RepairFormContainer.vue`의 자체 `<AppBar>`                                                                                              | Vote VT-Q1·Survey SV-Q1과 같은 계열. `repair.md` RP-Q2                                                                                                                                      |
| D-118 | 🔴 하자보수 수정 화면 콘텐츠 상단이 AppBar에 가린다                           | `RepairView/RepairFormContainer.vue:114` — `pt-12`가 없다. MovingHouse·소방·Repair 상세는 모두 준다                                                                                            | `repair.md` RP-Q3                                                                                                                                                                           |
| D-119 | 하자보수 폼이 Pinia 싱글턴이라 초안이 남는다                                  | `stores/repair.js` — `resetForm()`이 제출 성공·`그만두기`에서만 호출                                                                                                                           | 레이아웃 back·하드웨어 back으로 나가면 다음 진입에 이전 초안이 보인다. `repair.md` RP-Q5                                                                                                    |
| D-120 | 하자보수 등록·수정·취소 후 `invalidateQueries`가 없다                         | `usePostRepairSubmission.js` · `usePatchRepairSubmission.js` · `useDeleteRepairReceipt.js`                                                                                                     | 전부 `navigateBack()`이고 `staleTime: 0`이라 재조회된다. `repair.md` RP-Q7                                                                                                                  |
| D-121 | 하자보수 상태 필드명이 목록 `state` / 상세 `repairState`로 다르다             | `RepairListItem.vue:21` vs `RepairDetailContent.vue:118`                                                                                                                                       | 서버 계약 여부 확인 필요. `repair.md` RP-Q8                                                                                                                                                 |
| D-122 | `URL.createObjectURL`을 revoke하지 않는다                                     | `RepairView/RepairFormImage.vue:26` — computed 재평가마다 새 blob URL                                                                                                                          | 메모리 누수. `repair.md` RP-Q11                                                                                                                                                             |
| D-123 | 이미지 미리보기 `:key`가 서버 이미지에서 `undefined`                          | `RepairView/RepairFormImage.vue:56` — `image.name`은 File에만 있다                                                                                                                             | 기존 이미지 2장 이상이면 key 중복                                                                                                                                                           |
| D-124 | 하자보수 첨부 이미지에 확대(라이트박스)가 없다                                | `RepairView/RepairDetailContent.vue:105-118`                                                                                                                                                   | 썸네일만. `repair.md` RP-Q12                                                                                                                                                                |
| D-125 | `연락처`의 비활성 배경만 다른 회색                                            | `RepairView/RepairFormDetail.vue:72,90`(`-secondary`) vs `dong`·`ho`(`-mono`)                                                                                                                  | 세 필드가 나란히 비활성인데 하나만 다르다. `repair.md` RP-Q9                                                                                                                                |
| D-140 | 🔴 관리비 로딩 중 월 선택기가 2개로 보인다                                    | `ManagementFeeDetailView.vue` — 고지서 로딩 시 실제 월 선택기가 표시되는데 `ManagementFeeDetailLoading.vue:7-10`이 월 선택 스켈레톤을 또 그린다                                                | `management-fee.md` MF-Q4                                                                                                                                                                   |
| D-141 | 관리비 자동이체 칩 조건이 `autoTransfer !== 'N'`이다                          | `ManagementFeeDetailView.vue:41-46,135` — 필드가 없으면(`undefined`) 칩이 뜬다                                                                                                                 | `=== 'Y'`가 맞다. `management-fee.md` MF-Q5                                                                                                                                                 |
| D-142 | 관리비 증감 표시가 `!== null`만 검사한다                                      | `ManagementFeeDetailView.vue` — 필드가 `undefined`면 `▲ NaN원`이 보인다                                                                                                                        | `management-fee.md` MF-Q7                                                                                                                                                                   |
| D-143 | `DrawerMonth`가 `availableYearmonths` 참조 변경마다 `changeMonth`를 emit한다  | `components/common/DrawerMonth.vue` — refetch로 새 배열이 오면 사용자 선택이 최신 달로 되돌아간다                                                                                              | `management-fee.md` MF-Q8                                                                                                                                                                   |
| D-144 | 관리비 스켈레톤의 `상세내역` 헤더가 `border-b-2`, 실제는 `border-b`           | `ManagementFeeDetailLoading.vue:41` vs `ManagementFeeDetailView.vue`                                                                                                                           | 로딩→완료 시 1px 튄다. `management-fee.md` MF-Q3                                                                                                                                            |
| D-145 | 관리비 두 훅의 `queryKey`가 `getAptInfo()`에 옵셔널 체이닝을 안 쓴다          | `useGetManagementFeeImposeYearMonths.js:21,24` · `useGetManagementFeeBill.js:34,40` — `enabled`의 `hasManagementFeeContent`는 `?.`+`\|\| []`로 방어적인데 `queryKey`만 아니다                  | `queryKey`는 `enabled`와 무관하게 평가된다. `management-fee.md` MF-Q2                                                                                                                       |
| D-156 | 🔴 A-PASS 7초 타임아웃 경로에서 전역 로딩 플래그를 내리지 않는다              | `ApassView/ApassActivityButton.vue:23-35` — `isLoading`만 `false`로, `setIsApassLoading(false)`가 없다                                                                                         | `useNativeBackButton.js:31-34`가 이 플래그로 뒤로가기를 막으므로 **영구히 막힌다.** `apass.md` AP-Q3                                                                                        |
| D-157 | 🔴 A-PASS `invalidateQueries`가 v4 위치인자                                   | `lib/queries/apass/usePatchAPassActive.js:21-24`                                                                                                                                               | **UI 갱신 경로가 이것뿐이다.** v5 no-op이면 토글이 화면에 반영되지 않는다. `apass.md` §4-2                                                                                                  |
| D-158 | A-PASS 권한 표시가 브릿지 응답 전/웹 브라우저에서 전부 `허용안됨`             | `ApassView/ApassView.vue:20` — `permissionInfo = ref({})`                                                                                                                                      | 로딩 상태와 거부 상태를 구분하지 않는다. `apass.md` AP-Q4                                                                                                                                   |
| D-159 | 메인 A-PASS 배지 `사용중` 판정이 권한 2개만 본다                              | `MainView/MainCardMenu/MainCardApassBadge.vue:53-56` — `btOn && gpsEnabled` (AP1은 4개 표시)                                                                                                   | 위치 항상허용이 꺼져도 `사용중`. `apass.md` AP-Q5                                                                                                                                           |
| D-160 | `apassUseFlag: false`여도 `/apass`에 URL 직접 진입 가능                       | `router/ApassIndex.js` — 가드 없음                                                                                                                                                             | 미가입 상태로 토글을 누를 수 있다. `apass.md` AP-Q1                                                                                                                                         |
| D-176 | 🔴 `LayoutOpinionBase`가 2중 중첩 렌더된다                                    | `OpinionApp.vue:44-48`이 `LayoutOpinionBase`를 직접 렌더하고, `index-opinion.js:13`이 같은 컴포넌트를 라우트 컴포넌트로도 쓴다. `LayoutOpinionBase`에 `<slot>`이 없어 슬롯 자식 3개가 버려진다 | **`main`의 `pt-6`(24px)이 두 번 적용돼 48px = AppBar 높이가 되어 화면이 맞는다.** 중첩을 없애면 `pt-12`로 함께 고쳐야 한다. `showAppBar: true` 라우트에서는 AppBar가 2개. `opinion.md` O-Q6 |
| D-177 | 🔴 opinion 앱에 `ToastContainer`가 없어 모든 토스트가 무음이다                | `OpinionApp.vue:47`이 버려지는 슬롯 안. `LayoutBase`(메인)에만 렌더된다                                                                                                                        | `index-opinion.js`의 오프라인 안내(`네트워크 상태를 확인해주세요`)가 보이지 않는다. `opinion.md` O-Q7                                                                                       |
| D-178 | opinion 앱에 청크 로드 실패 복구·신규 배포 감지가 없다                        | `router/index.js`에만 `setupChunkReloadOnError`·`reloadIfNewVersion`이 있다                                                                                                                    | 외부 링크 사용자가 구버전 청크 요청 시 흰 화면. `opinion.md` O-Q8                                                                                                                           |
| D-179 | opinion 앱에 폰트 배율(`data-font-size`)이 적용되지 않는다                    | `LayoutBase`(메인)에만 있다                                                                                                                                                                    | 접근성 배율이 opinion에서 무효                                                                                                                                                              |
| D-161 | A-PASS 권한 항목에 클릭 동작이 없다                                           | `ApassView/ApassPermissionItem.vue` — `GO_APP_PERMISSION` 브릿지 호출부 0곳(D-18)                                                                                                              | 사용자가 직접 OS 설정으로 가야 한다. `apass.md` AP-Q2                                                                                                                                       |

## 아키텍처 개선 (전환 후 검토)

| #    | 항목                                                    | 근거                                                                                        | 비고                                                                                                       |
| ---- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| D-34 | 특정 단지(`SMA0002`) 전용 우회 코드                     | `MainView/MainView.vue:51` — 앱 비컨 갱신 이슈 우회                                         | 앱 이슈 해결 시 제거 가능. `features/main.md` M-Q1                                                         |
| D-35 | 서버 데이터(`residentDetailInfo`)를 localStorage로 복사 | `useGetResidentDetailInfo.js:102-123` — `authStore.setAptInfo(...)`                         | `04-state.md` 위반이지만 여러 곳이 `getAptInfo()`로 읽어 구조 변경 시 동작이 달라진다. Query를 단일 출처로 |
| D-36 | `useChangeApt`의 경쟁 조건                              | `lib/composables/useChangeApt.js:24-32` — 다른 훅의 `watch`가 이미 실행됐다고 가정하고 읽음 | v4 `invalidateQueries`와 겹쳐 있다. `features/main.md` §5 · M-Q2                                           |
| D-39 | 액세스 토큰을 URL 쿼리스트링으로 외부 사이트에 전달     | `MainNavigationSwiper.vue:48`(커뮤니티V2), `useShoppingNavigation.js:28`(쇼핑몰 토큰 4개)   | 히스토리·리퍼러·서버로그에 토큰 잔존. D-15·D-16과 같은 계열                                                |
| D-40 | 대부분 단지의 배너가 `mocks/BannerTemp.png`             | `MainAdvertisementBanner.vue:75`                                                            | 임시 이미지가 프로덕션에 노출. `features/main.md` M-Q4                                                     |

## 스타일 — 이식하며 발견 (Phase 4 2단계)

| #     | 항목                                                                     | 근거                                                                                                       | 비고                                                                                                                   |
| ----- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| D-184 | `.pretendard-20Regular`의 `fontFamily`가 소문자 `pretendard`             | `tailwind.config.js:489` — 나머지 66종은 `Pretendard`                                                      | CSS 폰트명은 대소문자를 구분하지 않아 **동작은 같다.** 이식 시 원본 그대로 뒀다                                        |
| D-185 | `@supports { .app { height: -webkit-fill-available } }` 규칙이 죽어 있다 | `input.css:30-34` — `.app` 클래스를 가진 요소가 `.vue` 306개에 **0곳**. 마운트 지점은 `#app`(id 선택자)    | iOS Safari 100vh 대응 의도였으나 선택자가 틀려 한 번도 적용되지 않았다. **이식하지 않았다.** 필요하면 새로 설계        |
| D-186 | `addComponents` 3종 전부 미사용                                          | `tailwind.config.js:808-815` — `select-background-position-custom`·`scroll-hidden`·`show-recent-entry` 0곳 | 이식하지 않았다                                                                                                        |
| D-187 | `@tailwindcss/typography` 플러그인이 미사용                              | `tailwind.config.js:817` — `prose` 클래스 0곳                                                              | 설치하지 않았다. Quill 본문은 `vue-quill.snow.css`가 담당한다                                                          |
| D-188 | `globalColor.scss` 66줄이 죽은 팔레트                                    | 이전 디자인 시스템의 SCSS 변수. `broken-styles.md` §5                                                      | 이식하지 않았다 (B-Q2 결정)                                                                                            |
| D-189 | 레거시가 v3 기본 팔레트를 8곳에서 직접 쓴다                              | `text-red-500`×5 · `bg-slate-100`×2 · `text-yellow-500`×1                                                  | 디자인 시스템 토큰이 있는데 기본 팔레트를 썼다. v4에서 값이 달라지므로 이관 시 hex로 고정한다(`tech-mapping.md` §10-1) |
| D-190 | Vue `<Transition>` 클래스 4세트가 프레임워크 규약에 묶여 있다            | `input.css:84-133` — `fade-*`·`slide-up-*`·`scale-*`                                                       | 타이밍·이징 값만 `@utility transition-*`으로 옮겼다. 실제 전환 구현은 Phase 4 7·9단계                                  |

## 서버·앱 계약 오타 (그대로 유지 — 앱·백엔드 협의 후 수정)

> **2026-07-29 결정 (사용자)**: 아래는 오타지만 **서버·앱이 그 철자를 기대**하므로 고치면 기능이 깨진다.
> 이관 시 **그대로 유지**하고, 나중에 앱·백엔드와 협의해 **함께 수정할 사항으로 남긴다.**
>
> ⚠️ CSS 클래스 오타는 성격이 다르다 — 프론트 단독으로 고칠 수 있고 고쳐야 한다 → `broken-styles.md`

| #    | 항목                                                                 | 근거                                                                                                  | 고치면 생기는 일                                                   | 협의 상대 |
| ---- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | --------- |
| D-9  | 관리비 조회 쿼리 파라미터 `startDateTIme`·`endDateTIme` (대문자 `I`) | `api/managementFee.js:20-21`                                                                          | **관리비 조회가 깨진다**                                           | 백엔드    |
| D-49 | 권한 필드 `locAlawaysOn` (`locAlwaysOn` 오타)                        | `constants/domain/apass.js:5`, `natives/common.js:117` — 원문 주석에 `네이티브와 함께 오타 정정 필요` | **위치 권한 상태를 못 받는다** → A-PASS 배지가 `권한없음`으로 고정 | 앱        |
| D-50 | 권한 필드 `btTransmitt` (`btTransmit` 오타)                          | `constants/domain/apass.js:6`                                                                         | **비컨 송신 상태를 못 받는다**                                     | 앱        |
| D-51 | 에러코드 `*_MISS_MATCH` (`MISMATCH` 오타)                            | `VOTER_MISS_MATCH`, `SURVEY_RESPONDENT_MISS_MATCH`                                                    | **에러 분기가 안 걸린다** → 기본 메시지로 폴백                     | 백엔드    |

> D-49·D-50은 레거시 코드 주석에 **"네이티브와 함께 오타 정정 필요"**라고 이미 적혀 있다.
> 앱 팀도 인지하고 있는 사항으로 보인다. 앱 프로토콜 변경 시 함께 처리한다.

## 보안 (Phase 0-3과 함께 검토)

> **2026-07-29 방침 변경**: 인증/로그인은 레거시 구현을 **그대로 이관**하기로 확정됐다
> (→ `decisions/auth-strategy.md`). 아래 3건은 전부 **전환 완료 후** 별도 작업이다.
> 이관 중에는 손대지 않는다.

| #    | 항목                                                | 근거                                                         | 비고                                                                                                                          |
| ---- | --------------------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- |
| D-15 | 아이디·비밀번호 평문 localStorage 저장              | `lib/composables/storage/useAuthStorage.js` — `userAuthInfo` | 자동 로그인이 이 값에 의존. D-17과 함께 처리해야 제거 가능                                                                    |
| D-16 | 미승인 입주민 조회가 비밀번호를 쿼리스트링으로 전송 | `api/auth.js:103-108`                                        | **E-Q2 확정: 그대로 유지.** POST body 전환은 백엔드 협의 필요                                                                 |
| D-17 | **쿠키 기반 refresh 전환**                          | 타깃 템플릿 `shared/lib/apiClient.ts`가 원래 이 방식         | 최초 계획이었으나 등가 이관 우선으로 보류. 전환 시 D-15·D-16이 함께 해소된다. 백엔드 협의 + 웹뷰 쿠키 실기기 검증이 선행 조건 |
