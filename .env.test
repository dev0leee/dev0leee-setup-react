# Vitest는 mode가 'test'라 .env.development를 읽지 않는다.
# 이 파일이 없으면 src/config/env.ts의 Zod 검증이 실패해 모든 테스트가 죽는다.
VITE_API_URL=http://localhost:8080/api
VITE_ENV=development
# node 테스트는 msw/node(setupServer)를 쓰므로 브라우저 워커는 끈다.
VITE_ENABLE_MSW=false
