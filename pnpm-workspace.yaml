# pnpm 11부터 빌드 스크립트 허용 목록은 package.json이 아니라 이 파일에서 읽는다.
# 여기서 true가 아닌 패키지의 postinstall은 실행되지 않는다(공급망 공격 방어).
allowBuilds:
  core-js: set this to true or false
  msw: true
  # eslint-import-resolver-typescript가 쓰는 네이티브 리졸버.
  # postinstall이 플랫폼별 바이너리를 링크하므로 빌드를 허용해야 동작한다.
  unrs-resolver: true
